# MidiSea 資料導演台 — IXD2026

**繁體中文** ｜ [English](./README.en.md)

線上版：<https://midisea.shyetech.com>　·　IxDA Taiwan 2026 會員工作坊「AI 共生黑客鬆」　·　12 組 卡加布列島（英文產品名：MidiSea Data Director）

用 **KORG nanoKONTROL2 / nanoPAD2** 兩台 MIDI 控制器（也可以用滑鼠、觸控、手機遙控、手把、相機手勢或語音），即時「演奏」一顆**化為海洋的 3D 星球**——透明球殼裡的療癒深海，內含水母、魚群、鯨豚、海龜與海洋垃圾，並有一片逐漸轉化成文字與數字的海。海況由台灣**政府開放資料**（水利署、中央氣象署；經 [Twinkle Hub](https://hub.twinkleai.tw/zh-TW) 探索資料集）驅動；另有一個「空氣品質」選項用的是 Open-Meteo 的 CAMS **模型資料**（不是政府觀測值，畫面上都會標示）。參數可 MIDI Learn 綁定、可錄製回放、可分享、可錄影下載；介面有**繁體中文 / English** 兩個語系。第一次進站有一步一步的互動新手導覽；整天不關機的展場有自動復原的防呆；換到新裝置前可以用內建的**裝置診斷頁**逐項確認硬體。

> 核心命題：把抽象的多維資料，變成一台可以用雙手演奏的資料樂器。

---

## 快速開始

```bash
npm install
npm run dev        # http://localhost:5173
npm test           # 單元測試 + i18n 驗收（node --test）
npm run i18n       # 只跑 i18n 報告：未包 t() 的中文、缺英文的 key
npm run build      # 產生 dist/
```

用**桌面版 Chrome / Edge** 開啟 http://localhost:5173，按「連線 MIDI」允許權限。

> Web MIDI 需要 Chrome / Edge + localhost 或 HTTPS；Safari（macOS 與 iOS）沒有 Web MIDI，Firefox 需另外授權且未驗證，詳見〈瀏覽器 / 裝置支援矩陣〉。內建瀏覽器沙盒會擋 MIDI 權限——實體裝置請用你自己的 Chrome。沒有硬體時，頂部「控制器」可用滑鼠鍵盤操作虛擬 nanoKONTROL2；iPad / iPhone 請用「多人」掃 QR 當遙控器。
>
> 相機（AR 實景、相機手勢）、麥克風（語音指令）與雙螢幕自動放置都需要 **HTTPS 或 localhost**。
>
> 要到現場、或換到一台沒用過的裝置之前，先開 `?diagnostics=1`（「裝置」面板也有連結）跑一次**裝置診斷頁**，逐項確認相機、麥克風、語音、震動、螢幕等能不能用，見〈裝置診斷頁〉。

---

## 主要功能

| 功能 | 說明 |
|---|---|
| 3D 海洋球 | Three.js / R3F；透明球殼 + 程序化波浪 + 海洋生物 + 流動文字；**滑鼠拖曳 / 旋鈕改變旋轉** |
| 中英雙語 | 頂部語言鈕（顯示 `EN` / `中文`）一鍵切換整站介面、日誌、資料名稱與分享文案；`?lang=en` / `?lang=zh` 可用網址指定。見〈語系〉 |
| 參數面板 | 分組滑桿，每個參數可 **MIDI Learn** 綁定 CC |
| 錄製 / 播放 | 把整段旋鈕演出錄下並回放；**soft-takeover** 播放中抓旋鈕平順接管、不跳值 |
| 分享 | 產生帶「資料脈絡」的網址：`?s=…`（視覺參數）＋ `&o=`（海況）`&m=`（月份）`&sl=`（鳥魚連動）`&lang=`（語系）；舊版只有 `?s=` 的連結照常可開。見〈分享〉 |
| 分享星球 | 擷取此刻的海合成分享卡（帶目前資料列）→ 手機系統分享 / 桌機下載 PNG，並附文案「我在 MidiSea 演了一片海 [網址]」（網址就是同一條帶脈絡的分享連結）。**實景（AR）時連相機拍到的真實背景一起輸出**：以 `object-fit: cover` 的幾何把相機畫面對位到畫布、套用與畫面相同的背景模糊 / 清澈濾鏡，再疊上透明的 WebGL 畫布（舊版 Safari 沒有 `ctx.filter` 時以縮放近似模糊）；「錄影」同理逐幀合成 |
| 資料導覽 | 按 **`T`**、資料卡的「▶ 開始導覽」，或閒置 30 秒：依序巡演八站（今日水庫 → 花蓮潮汐 → 月亮 → 雲林揚塵 → **空氣品質（模型資料）** → 鳥群 → 魚群 → 河川測站星座），畫布下方大字幕用當下的真實資料說明「現在看的是什麼、球為什麼長這樣」，並誠實點出資料限制；整輪約 111 秒，碰任何東西就停並還原。**導覽員控制**：進度點可點跳站、上一站 / 下一站 / 暫停（鍵盤 `←` `→` `P`）、「複製此站連結」（`?tourstop=`）、選用的語音旁白。見〈資料導覽〉 |
| 新手導覽 | 第一次進站出現 7 步互動導覽（半透明遮罩 + 聚光框 + 提示卡，每步一兩句話），取代過去一進站就跳出的長篇說明。完整說明仍在「說明」鈕後面，重整成「快速上手 5 條 + 6 個預設收合的主題區塊 + 海洋生態區塊」，並可按「重新看新手導覽」。`?onboard=0` / `?onboard=1` 可關 / 強制顯示。見〈新手導覽、系統事件監看與視角取景〉 |
| 系統事件監看可隱藏 | 畫面下方的「輸入輸出監看（IN 演奏者 / OUT 系統事件）」可用標題列的小箭頭、Footer 的「系統事件」開關或鍵盤 **`L`** 隱藏；桌面預設顯示、手機預設隱藏，切換後記住。隱藏時 Footer 仍用一行小字顯示最新一則 OUT 事件，日誌照常累積。`?log=1` / `?log=0` 只覆寫這一次 |
| 視角自動取景 | 畫布窄長（例如手機直式全螢幕的演出模式）時，相機自動拉遠讓整顆球看得到；橫式、桌面與接近正方形的畫布維持原本取景、完全不變。縮放滑桿與雙指縮放照常可用；`?fit=0` 關閉 |
| 裝置診斷頁 | `?diagnostics=1`（「裝置」面板有連結）：25 項一鍵快速檢查 + 12 項按鈕才啟動的互動檢查（相機、麥克風、語音、MIDI、手把、震動、觸控筆、感測器、螢幕、彈出視窗、全螢幕），可複製 / 下載報告；不上傳任何東西。見〈裝置診斷頁〉 |
| 展場防呆 | 畫面出錯時倒數自動重新載入（5 → 15 → 60 秒退避，10 分鐘內 5 次即熔斷停手）、WebGL 遺失復原、渲染看門狗、新版與新資料自動更新（閒置時才動）、`?reload=HH` 每日重載；「裝置 → 維運」可看版本、崩潰紀錄與啟用的防呆。見〈展場防呆與維運〉 |
| 點物件看資料出處 | 輕點背景上的測站星 / 月亮 / 球外鳥群，在物件旁彈出資料卡（來源、數值、狀態）。見〈點物件看資料出處與觸控筆〉 |
| 觸控筆 | 用 Apple Pencil / Surface Pen / Wacom 拖曳球體：壓力放大浪勁、筆身傾斜推動洋流方向（僅 `pointerType === 'pen'`；`?pen=0` 關閉） |
| 多人合奏 | 按「多人」秀 QR，手機掃碼變遙控器（PeerJS WebRTC，短 ID → QR 小而好掃）；每支手機分到「聲部」（海 / 生態 / 氛圍 / 自由）、滑桿每秒跟主畫面同步；**手機感測器**：啟用後傾斜=洋流方向、搖晃=浪湧（走同一條 wire 協定）。全部走同一條 `input()` 路徑（可錄製、可 soft-takeover）。host 與訊號伺服器斷線會用同一 ID 自動重連、被銷毀則換號重建 |
| 「裝置」面板 | 頂部「裝置」鈕：把選用的裝置功能集中一處——**觀眾視窗（雙螢幕）**、**相機手勢**、**語音指令**、**觸覺回饋**、**畫質**，以及**裝置診斷**（診斷頁連結與上次結果）、**維運**（版本、崩潰紀錄、防呆狀態）兩節；相機 / 麥克風只在你打開時才啟用，畫面與聲音都不上傳。見〈「裝置」面板〉 |
| 真實資料 | 水庫水位 % **直接等於海水高度**（滿庫=滿球，>97% 液體從球外緣溢流、球底積水痕）；石門 / 德基 / 曾文附 24h 進流量、**花蓮外海附 CWA 潮汐**時間序列，可「▶ 播放資料」讓球隨真實資料起伏（**倍速 ×0.5–×4、循環**，畫布上方 HUD 顯示資料時刻與數值）；另有「**揚塵 · 雲林縣**」「**月亮 · 花蓮**」「**空氣品質 · 雲林**」三個海況，全部資料集見下方〈資料集 → 畫面〉 |
| 空氣品質（模型資料） | 「空氣品質 · 雲林」海況：Open-Meteo Air Quality API（CAMS 全球大氣模型）在雲林縣麥寮的逐時 PM2.5、PM10 與 US AQI。**這是模型資料，不是政府觀測值**（資料卡的琥珀色標籤、資料看板、OUT 日誌都標示「模型資料」；資料卡另有一行可見的說明：約數十公里的粗網格模型估計，可能與地面測站數值不同，請勿當官方空品判讀）。「▶ 播放空氣品質 120 小時」以每小時 0.2 秒回放最近 5 天：PM2.5 越高，海水越混濁、垃圾越多、色相偏黃綠、輝光收斂。CC BY 4.0，需標示 Open-Meteo（資料卡有連結）。見〈資料集 → 畫面〉 |
| 銀河 × 河川 | 背景帶狀銀河，濃度由**水利署即時河川水位**（5 站 / 警戒比）驅動——水豐則銀河更亮 |
| 鳥群 / 魚群 × 調查資料 | 球外線稿鳥群數 =「鳥群數量」參數（0..1 → 0..5 群）；魚群同理走「魚群數量」。資料卡顯示該流域**逐月物種數長條圖**（斜線＝內插、點長條預覽月份、標示現在），並給資料建議值，你可以「**套用資料**」、開「**連動**」（換海況 / 換月份時自動更新），或直接拖曳滑桿「**獨立控制**」（手動調會自動脫鉤，參數列的「資料」標籤消失）。每個流域另有**調查年表**（時間軸）可「▶ 播放」：每一步＝一個日曆年，數量隨當年物種數變化，沒有調查的年份標為「無調查」並以前後調查年內插補上。注意（資料誠實聲明）：各流域僅部分月份 / 年度有調查（每月只涵蓋 1–3 個年度，近似單一年份快照，非氣候平均），只收紀錄數達門檻的月份，缺月以相鄰月份**環狀內插**並標註「內插」。見〈調查空窗時間軸〉 |
| 月亮 × 潮汐 | 潮汐海況時背景出現月亮：**盈虧＝當日月齡（由 CWA 農曆日期推得，與潮汐資料同源）**、位置＝月中天時刻與當下時刻決定的天空弧線（新月正午 / 上弦傍晚 / 滿月午夜中天）。資料播放 24h 潮位時月亮真的東升西落，HUD 同步顯示乾/滿潮標記、農曆、潮差、月相。另有獨立海況「**月亮 · 花蓮**」：以 CWA A-B0063-001 的月出 / 中天 / 月沒時刻、月出月沒**方位角**與中天**仰角**把月亮放在真實的方位與高度（閒置＝今天 × 現在時刻內插；「▶ 播放月出月沒」每一步＝一天，月亮每晚 21:00 的位置與盈虧逐日推進，中天越高海水越高＝示意） |
| 資料看板 · 輸出顯示資料 | 畫布左上「資料看板」列出目前海況背後的真實資料與映射（水庫 / 潮汐 / 揚塵 / 空氣品質 / 氣象 / 鳥 / 魚 / 河川 / 測站，手機預設關）；**OUT 監看**在套用海況時輸出「資料 …」列、資料播放**每一步**輸出「DATA …」行；「分享星球」圖片也帶資料列；匯出 LOG 一併包含 |
| 畫面資訊面板控制 | 畫布上的資訊面板分三組、各自可關：**資料看板** / **播放與參數提示**（資料播放進度、參數數值、待接管旋鈕提示、AR 調整鈕、聲音提示、導覽字幕、資料出處卡片）/ **掃碼 QR 與統計**（演出模式）。頂部「**資訊**」鈕或鍵盤 **`I`** 一鍵全部隱藏 / 顯示（再按一次還原原本的組合）；看板右上 **×**；資料卡「畫面顯示」三個勾選分別開關；偏好存 localStorage。網址覆寫 `?hud=0`（看板+提示）/ `?board=0` / `?qr=0`（給展場、錄影用，不會蓋掉使用者偏好）。隱藏的面板直接卸載（不空跑動畫迴圈）。相機手勢與語音的「使用中」徽章是隱私提示，**不受此開關控制、永遠顯示** |
| 背景模糊 / 清澈 | 「VIEW」的兩支滑桿（可 MIDI Learn / 錄製 / 手機遙控）：**只作用背景**（星空 / 銀河 / 流星 / 月亮 / 測站星座，AR 時＝相機畫面），球體與生物保持清晰。實作：背景放 layer 1 → 先畫背景 → 複製畫面 → Kawase 雙濾波（多層降 / 升採樣，近似高斯、稀疏星點不會重影）→ 蓋回（含清澈度）→ 再疊前景；預設值零額外成本、GPU 不支援時自動降級；低畫質時改用簡化暗化（見〈畫質〉） |
| 預設聲音開啟 | 瀏覽器規定音訊要有使用者手勢才能出聲，所以「預設開啟」＝**第一次點 / 觸碰 / 按鍵時自動啟動**（畫面有「點一下畫面即開啟聲音」提示）；按「聲音」靜音後記住、之後不再自動開。展場無人操作時請先點一下，或以 `--autoplay-policy=no-user-gesture-required` 啟動 Chrome |
| 手機 UI | ≤820px：頂部拆成錄放 / 場景 / **可橫向捲動的工具列**（按鈕不折行、觸控目標 ≥40px、優先序：聲音 → 分享 → 多人 → 實景…）、參數列兩行式（滑桿獨佔整行）、觸控大顆滑桿、`select` 16px（iOS 不會自動放大）、輸入輸出監看預設隱藏（Footer 的「事件」開關可打開，打開後只顯示 OUT 欄）、預設畫布 40%；桌面 821–1400px 收緊間距維持單行 |
| 物理垃圾 | cannon-es 剛體：瓶 / 袋浮在水面、互相碰撞、被洋流推、被**淨化波推開** |
| AR 實景 | 「實景」以相機當背景（球體浮在真實世界）；**高斯模糊 / 清澈滑桿只作用背景**，球體照常渲染；**環境光感知**：每 0.6 秒取相機平均亮度自動調球體輝光（環境亮 → 輝光強、暗 → 收斂；手動調輝光後暫停 8 秒，可關閉） |
| Marker 快照 | nanoKONTROL2 Track ◀▶＝上/下一場景；Marker Set＝存當下狀態為快照（最多 8 組）、Marker ◀▶ 巡回 |
| 展場模式 | 網址 `?kiosk=1` 直接進演出模式（不彈說明視窗、也不出現新手導覽）＋**閒置自動資料導覽**；**角落常駐小 QR**（不必開視窗，觀眾走過隨掃隨入；斷線 / 開機無網路都會自動補啟動並重畫）+ 統計「合奏 N 人 · 演出 M 次」；網址可加 `&hud=0`（不顯示資訊面板）、`&qr=0`（不顯示 QR）、`&reload=3`（每天 03:00 閒置時重載一次）。展場模式同時啟用渲染看門狗與較密的版本 / 資料檢查，見〈展場防呆與維運〉。搭配「裝置」面板的**觀眾視窗**可在投影機全螢幕顯示同一片海（觀眾視窗預設也有看門狗） |
| 場景配色 | 每個場景一組色相（湛藍 / 紫粉 / 碧綠 / 濁綠 / 青藍），水線、粒子、大氣、霧一起轉調；「海色色相」也是可演奏參數。**海色校正**：`setHSL` 改以 sRGB 解讀（第 4 參數 `THREE.SRGBColorSpace`），背景與霧由偏亮的灰藍板岩色改為接近設計稿的深藍海（夜間約 `#050e1c`、白天約 `#08162b`），水線亮度保留約 85%；色相拉到極端值（0 / 1）時背景會偏深綠 / 深梅紅 |
| Gamepad / 指北針 | 手把左桿=洋流方向、右桿=自轉與海水高度、A/B/X/Y=召喚；手把可有**震動回饋**（見〈觸覺回饋〉）；手機轉身（羅盤 >8°）=洋流轉向 |
| PWA | 「加入主畫面」安裝提示 + 部署新版時「點此更新」toast；離線可開 |
| 錄影 | 擷取星球 10 秒，下載 **MP4 / WebM**（實景時錄「相機背景 + 球體」） |
| 匯出 LOG | 下載 IN/OUT 訊息日誌，方便開發者除錯 |
| 舒適背景音 | Tone.js 生成式環境音：海水高度=根音 Hz、清澈=明亮度、洋流=浪速、輝光=空間感、垃圾=失諧、魚群×游速=點綴音、鯨豚龜=叫聲、打擊墊=音階（按「聲音」開啟）；**淨化波 → 明亮大調五聲上行琶音**（不論當下濁度都是大調）、**溢流（水位 >97%）→ 水滴聲**（隨機左右聲道，最多約 3 滴/秒；排程時間單調遞增，避免高更新率螢幕上 Tone.js 丟「time must be >= last scheduled」而讓主迴圈停擺；音訊更新出錯不再往上拋，主迴圈先排好下一幀） |
| 可調版面 | 拖曳分隔線改變面板寬度 / 監看高度 |
| 手機 RWD | 窄螢幕自動改為上下堆疊 + 觸控互動 |
| 本機儲存 | localStorage / sessionStorage 記住參數、綁定、尺寸、錄製、日誌 |
| AR 桌面（WebXR） | 在支援 WebXR 的手機（Android Chrome 等）打開「裝置」→「AR 桌面」→「在桌面上放置」：用鏡頭找到桌面，點一下就把約 28 公分的海放在桌上，繞著走各角度都看得到；iOS Safari 沒有這個功能，請改用「實景」。**尚未在真機驗證**，詳見〈「裝置」面板〉 |

---

## 控制對應（nanoKONTROL2 出廠預設）

**6 推桿 (Slider 1–6, CC0–5)**

| 推桿 | 效果 |
|---|---|
| 1 | 海水高度 |
| 2 | 洋流速度 |
| 3 | 水母數量 |
| 4 | 魚群數量 |
| 5 | 垃圾數量 |
| 6 | 海水清澈程度 |

**2 旋鈕 (Knob 1–2, CC16–17)**：旋轉海洋球 / 生物游動速度。

**4 按鈕 (Button 1–4, Solo CC32–35)**：鯨魚出現 / 海豚出現 / 海龜出現 / 清除垃圾。

**走帶鍵**：● 錄製開始/結束、▶ 播放/停止、■ 停止、Cycle 清除錄製——演出全程不碰滑鼠。

**LED 回饋**（nanoKONTROL2 需以 KORG Kontrol Editor 設 LED Mode = External）：錄製中 ● 閃、播放中 ▶ 亮、待播放 ■ 亮、Learn 模式 Cycle 閃、soft-takeover 待接管時該軌 R 鍵閃、觸發鍵按下亮 0.3 秒。

**藍牙 MIDI（Web Bluetooth）**：「藍牙 MIDI」鈕直接以 GATT 連 BLE-MIDI 控制器 / 鍵盤（不必先在系統配對），與 USB 共用同一條 `routeMidi` 路徑，Learn / 錄製 / soft-takeover 通用。支援 Chrome / Edge（桌面、Android）；**iPad / iPhone 的 Safari（與其他 iOS 瀏覽器）沒有 Web Bluetooth，也沒有 Web MIDI**——那些裝置請用「多人」掃 QR 當遙控器（WebRTC，不需藍牙）。

**nanoPAD2 X-Y 觸控板**：X（Pitch Bend）→ 洋流向 X（攪動浪的方向）；Y 預設不綁（點「洋流向 Y」→ 滑觸控板即 Learn）。

**nanoPAD2 打擊墊：16 效果 × 4 bank（=64 墊）**：`note % 16` 選效果、`note ÷ 16` 選強度檔位（弱 / 中 / 強 / 爆，對應 nanoPAD2 Scene 或音高區段），velocity＝力度。效果表：水母脈衝 / 浪湧 / 漣漪 / 氣泡 / 亮星 / 海豚 / 鯨魚 / 海龜 / **淨化波** / 垃圾投放 / 洋流轉向 / 閃光 / 魚群衝刺 / 漣漪三連 / 星雨 / 大浪+氣泡。虛擬控制器有相同的 16 墊 + bank 切換。

**手機觸控／感測**：左右拖曳=旋轉（放手有慣性）、上下滑=海水高度、兩指縮放=遠近、點擊=亮星爆發、**長按 0.5 秒=魚群聚集到手指**、**傾斜手機=水面保持水平**、**搖晃=攪動海水**（iOS 首次觸碰會請求感測權限）。可「加入主畫面」以 PWA 全螢幕離線使用。

> **生態連動**：垃圾增加 → 海水混濁、生物減少、**魚群主動繞開垃圾**、點綴音轉小調；清除垃圾 → **淨化波**擴散、生物回歸、聲音回到大調。背景另有 4 分鐘一輪的極慢晝夜。
> 若你的裝置 CC 不同：**點參數名稱 → 轉旋鈕**即重綁（Learn），或按「依序對應旋鈕」；`shift+點` 解綁。按鈕也可用畫面上的動作鈕（滑鼠 / 觸控）。綁定存 localStorage。

---

## 鍵盤快速鍵

| 按鍵 | 作用 | 備註 |
|---|---|---|
| `H` | 演出模式（隱藏全部介面，只留球體）；再按一次離開 | 雙擊畫面也是同一個開關 |
| `I` | 顯示 / 隱藏畫布上的資訊面板（資料看板、播放與參數提示、QR） | 再按一次還原原本的組合 |
| `T` | 開始 / 停止資料導覽 | 帶 Ctrl / Cmd / Alt 時不算 |
| `L` | 顯示 / 隱藏畫面下方的輸入輸出監看（系統事件） | 帶 Ctrl / Cmd / Alt 時不算；焦點在輸入框 / 下拉選單、或在彈窗（說明 / 裝置 / 多人）內時不切換（焦點在按鈕上仍會切換）；桌面預設顯示、手機預設隱藏，切換後記住。導覽進行中按 `L` 也算操作，會中止導覽並還原 |
| `←` `→` `P` | **資料導覽進行中**：上一站 / 下一站 / 暫停·繼續 | 只在導覽進行中有效，不會中止導覽；按住不放不會連續跳站；焦點在滑桿（例如虛擬控制器的旋鈕）或輸入框時交給該元件 |
| `→` / `Enter` · `←` · `Esc` | **新手導覽開著時**：下一步 · 上一步 · 略過 | 導覽卡片是彈窗，焦點在卡片內時全域快速鍵略過；`Tab` 只在卡片的按鈕之間循環 |
| `?` | 開啟說明視窗 | |
| `空白鍵` | 播放 / 停止播放 | 按鈕聚焦時交給按鈕原生行為 |
| `R` | 錄製開始 / 結束 | 按鈕聚焦時交給按鈕原生行為 |
| `1` `2` `3` `4` | 鯨魚 / 海豚 / 海龜 / 清除垃圾（淨化波） | 按鈕聚焦時交給按鈕原生行為 |
| `Esc` | 停止資料導覽並還原；關閉「資料出處」卡片 | |
| `方向鍵` | 在虛擬控制器上點選控制項後微調（每次 ±0.05） | |
| `Enter` / `空白鍵` | 參數名稱上聚焦時＝開始 MIDI Learn（`Shift` 加上＝解綁） | |

在輸入框、下拉選單聚焦時，全域快速鍵一律放行（不攔截）。資料導覽進行中，除了 `T` `H` `I` `?`、導覽員快速鍵 `←` `→` `P`、修飾鍵與 `Tab` 之外的按鍵都會中止導覽並還原（`Esc` 是結束）。

---

## 網址參數

| 參數 | 值 | 作用 | 備註 |
|---|---|---|---|
| `?s=` | base64url | 視覺參數（每個參數量化成 1 byte，依 `PARAM_ORDER` 順序） | 「分享」產生；位置編碼不能改，舊連結靠它解碼 |
| `&o=` | 海況選項 id：`feitsui` `shimen` `deji` `zengwen` `nanhua` `hualien-tide` `dust-yunlin` `moon-hualien` `air-yunlin` | 開啟時直接選好該海況 | 不存在的 id 會被忽略 |
| `&m=` | `0`–`11`（0＝1 月） | 手動預覽的調查月份 | 分享者沒手動選過就不帶 |
| `&sl=` | `b0f0` `b0f1` `b1f0` `b1f1` | 鳥（b）/ 魚（f）是否連動調查資料，例如 `b1f0`＝鳥連動、魚獨立 | 只暫時套用、不寫進對方的 localStorage 偏好 |
| `?lang=` | `zh` / `en`（容忍大小寫與區域碼，如 `en-US`、`zh-TW`） | 介面語言 | 優先於偏好與瀏覽器語言；不寫入偏好；按語言鈕時網址會同步更新 |
| `?kiosk=1` | `1`（`0` `false` `off` `no` 為關） | 展場模式：直接進演出模式、不彈說明視窗、不出現新手導覽、角落常駐 QR、閒置自動導覽開（可用 `?tour=0` 關）、啟用渲染看門狗 | 只有 `?kiosk=0` / `false` / `off` / `no` 明確關閉，其餘寫法（`?kiosk`、`?kiosk=1`…）都算開；導覽、觀眾視窗、防呆共用同一個判斷 |
| `?hud=0` | `0` | 隱藏資料看板 + 播放與參數提示 | 展場 / 錄影用，不寫入偏好 |
| `?board=0` | `0` | 只隱藏資料看板 | 同上 |
| `?qr=0` | `0` | 隱藏展場 QR 與統計 | 同上 |
| `?tour=` | `0` `1`（也認 `off` `on`） | 強制關 / 開「閒置自動導覽」 | 優先序：`?tour=` > `?kiosk` > 使用者偏好 > 預設（開） |
| `?audience=1` | `1` | 觀眾視窗模式（無控制介面、無聲音，同步主視窗） | 一般由「裝置」面板的按鈕開啟；手動開啟時會等待主視窗；預設啟用渲染看門狗（投影機整天開著、卡死沒人發現；`?watchdog=0` 可關） |
| `?pen=0` | `0` | 關閉觸控筆的壓力 / 傾斜表現 | 沒有裝置面板開關，只有這個參數 |
| `?fps=1` | `1` | 在畫布左下角顯示 FPS 小字（除錯用） | 沒有這個參數時畫布不會顯示任何 FPS |
| `?quality=` | `auto` `high` `medium` `low` | 本次強制指定畫質 | 不寫入偏好；在面板選擇才會改存偏好 |
| `#remote=<id>` | hash（不是 query） | 手機遙控頁（掃「多人」QR 得到） | 主畫面產生 |
| `?diagnostics=1` | `1`（`0` `false` `off` `no` 為關） | 裝置診斷頁（獨立頁面，不載入 3D 場景與主畫面） | 「裝置」面板的「開啟裝置診斷」會開新分頁；頁面路由優先序：手機遙控頁 `#remote=` > 診斷頁 > 觀眾視窗 > 主畫面。見〈裝置診斷頁〉 |
| `?log=` | `1` `0`（也認 `true` `on` `yes` `show`、`false` `off` `no` `hide`） | 這一次強制顯示 / 隱藏輸入輸出監看（系統事件） | 不寫入偏好；優先序：網址 > 使用者偏好 > 依寬度預設（>820px 顯示、≤820px 隱藏） |
| `?onboard=` | `1` `0`（`0` `false` `off` `no` 為關，其餘算開） | 強制顯示 / 不顯示新手導覽 | `?onboard=1` 蓋過「已看過」與 `?kiosk`；手機遙控頁、觀眾視窗、診斷頁一律沒有新手導覽（連 `?onboard=1` 也不行）；`?kiosk` 與 `?tourstop=` 預設不顯示 |
| `?fit=0` | `0`（`false` `off` `no` 同） | 關閉視角自動取景 | 除錯用；預設開，寬螢幕本來就不變 |
| `?watchdog=` | `1` `0` | 強制開 / 關渲染看門狗 | 預設：`?kiosk` 與觀眾視窗開、其餘關；`?watchdog=0` 一律關 |
| `?autoupdate=0` | `0`（`false` `off` `no` 同） | 偵測到新版時只記錄、不自動重新載入 | 預設：正式建置有新版且閒置時自動重載；開發版（dev）不做版本檢查 |
| `?reload=HH` | `0`–`23` 的整數 | 每天本地時間 HH:00 之後、閒置時重新載入一次 | 頁面要一直開著才有效；不是 0–23 的值會被忽略；「裝置 → 維運」會顯示 |
| `?tourstop=` | `1`–`8`，或站 id：`reservoir` `tide` `moon` `dust` `air` `birds` `fish` `stations` | 資料載入後直接從那一站開始資料導覽（只啟動一次） | 不合法的值（例如 `zzz`、`9`）不啟動導覽；有這個參數時不顯示新手導覽；「複製此站連結」產生的網址用站 id |
| `&tourhold=1` | `1`（`0` `false` `off` `no` 為關） | 配合 `?tourstop=`：到站後停在暫停狀態（導覽員模式） | 沒有 `?tourstop=` 時沒有作用 |
| `?speak=` | `1` `0`（也認 `on` `true` `yes`、`off` `false` `no`） | 導覽的語音旁白預設開 / 關 | 預設關；優先序：網址 > 使用者偏好 > 關；只在主視窗出聲，瀏覽器不支援語音合成時沒有作用。見〈資料導覽〉 |

範例：

```text
https://midisea.shyetech.com/?kiosk=1&lang=en&hud=0                展場（英文、不顯示資訊面板）
https://midisea.shyetech.com/?kiosk=1&reload=3                     展場（每天 03:00 閒置時重載一次）
https://midisea.shyetech.com/?s=…&o=zengwen&m=8&sl=b1f0&lang=zh    「分享」產生的連結（&m=8 是 9 月）
https://midisea.shyetech.com/?tourstop=air&tourhold=1              導覽員連結：直接開在「空氣品質」站並暫停
https://midisea.shyetech.com/?diagnostics=1                        裝置診斷頁
https://midisea.shyetech.com/?quality=low&fps=1                    低階裝置除錯
```

---

## 錄製 / 播放

1. `● 錄製` → 動旋鈕 / 拖球體 / 拉滑桿演出 → `■ 停止錄製`
2. `▶ 播放` 重現整段；`⟲ 清除` 重錄
3. **soft-takeover**：播放時抓實體旋鈕，需先讓實體值「經過」畫面當下值才接管，避免跳值；手動拉滑桿 / 拖球體則即時接管
4. 錄製自動存 localStorage，重開仍可播放

## 分享

按 `分享` → 複製一條帶目前海況的網址：

```text
?s=<視覺參數>&o=<海況>&m=<調查月份>&sl=<鳥魚連動>&lang=<語系>
```

除了畫面參數，還包含你選的海況（水庫 / 潮汐 / 揚塵 / 月亮 / 空氣品質）、手動預覽的月份、鳥魚是否連動調查資料，以及介面語言。對方開啟就會看到與你一樣的畫面與資料脈絡（載入順序：海況選項 → 月份 → 鳥魚連動 → 視覺參數 → 語系；視覺參數最後套，不會被海況預設值蓋掉）。

- 連動狀態與語系只「暫時套用」，不會改掉對方自己的偏好（重新整理不帶參數的網址就回到對方原本的設定）。
- 舊版只有 `?s=` 的連結永遠相容；亂碼或不存在的欄位只忽略該欄，不影響其他欄位。
- 展場網址（只帶 `?lang=` 或 `?kiosk=1`）不算分享連結，首次到訪仍以今天真實的海開場。

按 `分享星球` → 產生 1080×1080 分享卡，附文案「我在 MidiSea 演了一片海 [網址]」（英文：「I played a sea in MidiSea [url]」）。手機走系統分享（同時帶圖片與文字）；瀏覽器不支援檔案分享時（多半是桌機）改下載 `midisea-star.png`，並把文案與連結複製到剪貼簿（瀏覽器拒絕剪貼簿時靜默略過，只提示已下載）。英文版的網址列與資料列會先縮字級、再截斷，不溢出卡片。

## 錄影

按 `錄影` → 擷取星球 10 秒 → 自動下載 `ixd2026-globe.mp4`（瀏覽器不支援 MP4 時退回 `.webm`）。

## 匯出 LOG

按 `匯出LOG` → 下載 `ixd2026-log.txt`（帶時間戳的 IN/OUT 訊息），供除錯 MIDI / 事件流。

---

## 資料導覽

讓不會操作的觀眾也能「看懂」這片海：程式自己一站一站巡演真實資料，並用字幕解釋。導覽員也可以像簡報一樣掌控節奏：跳站、暫停、把某一站的連結傳給別人。

**怎麼開始**：鍵盤 **`T`**；面板「資料導覽」卡片（在資料卡之後）的「▶ 開始導覽」；或閒置 30 秒自動開始（預設開，可在卡片取消勾選「閒置 30 秒自動導覽」，偏好存在瀏覽器；`?kiosk=1` 預設開，`?tour=0` / `?tour=1` 強制關 / 開，優先於偏好與 `?kiosk`）。

**八站**（缺資料的站自動略過；AR 實景時略過看不到的月亮與星座站；整輪約 111 秒）：

| 站 | 內容 |
|---|---|
| 今日水庫 | 水位 % → 海水高度 |
| 花蓮潮汐 | 播放 24h 潮位，字幕帶潮差（大潮 / 小潮…）、農曆與月相 |
| 月亮 | 月亮在天空移動，字幕帶今日月出 / 月沒 |
| 雲林揚塵 | PM10 → 海水濁度；PM10 感測器無效時誠實說明「改看風速」，來源疑似凍結、歷史累積中也會明講 |
| 空氣品質 | Open-Meteo / CAMS 模型的逐時 PM2.5：PM2.5 越高，海水越混濁、垃圾越多；字幕明講「模型資料，非政府觀測」 |
| 鳥群 | 調查年表逐年播放，字幕列出無調查（內插）的空窗年 |
| 魚群 | 同上（例：曾文溪 2007–2013 無調查） |
| 河川測站星座 | 188 座河川流量測站 |

**畫面**：畫布下方的大字幕（標題 1 行 + 說明最多 2 行、淡入淡出），下方是可點的進度點（n/N）與一列導覽員按鈕。字幕只存資料、顯示時才依當下語系翻譯，所以導覽中途切換語言字幕會立刻換。短螢幕橫向（例如手機橫放）時，字幕改成靠左下的窄卡，不蓋住球的下半。屬「播放與參數提示」那一組（`I` 關閉時字幕隱藏，導覽照跑）。

**導覽員控制**（導覽進行中；導覽員按鈕、進度點與「複製連結」都不算「操作海」，不會中止導覽）：

- **進度點**：點任一點跳到那一站，字幕與海況立即套用，導覽繼續、不切換演出模式。
- **按鈕列**：上一站、暫停 / 繼續、下一站、複製此站連結，以及（瀏覽器支援語音合成時才出現的）念出字幕。字幕卡與面板的「資料導覽」卡片各有一組。
- **鍵盤**：`←` 上一站、`→` 下一站、`P` 暫停 / 繼續；`Esc` 仍是結束並還原。
- **暫停語意**：暫停會凍結該站的計時與資料播放（字幕卡邊框變琥珀色並出現「已暫停」標籤，資料 HUD 也顯示「已暫停」，而不是極小的倍速）；暫停中在導覽內跳站，新的一站也維持暫停；繼續時從該站剩下的時間接著走。**暫停中碰任何東西**（點畫面、動旋鈕、按其他鍵）**仍會結束導覽並還原原本的海**，播放倍速也還原。**自動導覽（閒置啟動 / 展場）的暫停有 3 分鐘逾時**：展場上有人按了暫停就走開，導覽不會整個下午定格在同一站——3 分鐘內沒有任何導覽員操作（暫停、上一站 / 下一站、點進度點）就自己繼續；暫停中換站或再按暫停會重新計時。**手動啟動的導覽與導覽員模式（`?tourhold=1`）不逾時**，暫停多久都行，直到你按 `P`、碰任何東西或 `Esc`。自動導覽每輪回到第 1 站時會用當下的資料重建站表，資料更新（每 30 分鐘 / 3 小時）之後字幕的數字才會跟球實際套用的資料一致；手動導覽的站表整輪不變。
- **每站連結**：「複製此站連結」複製 `https://…/?tourstop=<站 id>`（暫停中另帶 `&tourhold=1`，英文介面另帶 `&lang=en`），按鈕短暫變成「已複製」，OUT 監看多一行紀錄。連結只含站 id、語言與暫停旗標，**不會夾帶**目前網址上的其他參數（`?kiosk`、`?audience`、`?s=`、`#remote=` 之類）。別人開啟後，資料載入完成就從那一站開始（只啟動一次）；帶 `tourhold=1` 則停在暫停狀態；這種連結不會先跳出新手導覽擋住。`?tourstop=` 認 `1`–`8` 或站 id（`reservoir` `tide` `moon` `dust` `air` `birds` `fish` `stations`）；不合法的值不啟動導覽；連結的站這次因缺資料或 AR 實景被略過時，從第 1 站開始。連結用站 id 而不是序號，是因為缺資料或 AR 實景會略過某些站，序號會位移。
- **觀眾視窗**：顯示同樣的字幕與進度，但進度點不能點、沒有按鈕列；主視窗暫停時顯示「已暫停」並停住進度條。

**語音旁白（選用）**：預設關，開啟前不會要求任何權限。開啟後（按鈕列的「念出字幕」，或網址 `?speak=1` 預設開；優先序：網址 > 偏好 > 關），每換一站就用瀏覽器內建的語音合成（Web Speech `speechSynthesis`）念出該站字幕：中文用 `zh-TW`、英文用 `en-US`，找不到時退到同語系的其他聲音；單位與符號會先口語化（例如「μg/m³」念成「微克每立方公尺」）。

- 只在主視窗出聲，觀眾視窗不出聲；暫停或結束會立刻停止，暫停後繼續、換站、切換語言都會重念目前這一站。該站時間到而句子還沒念完時，最多再等 6 秒才換站。
- **旁白進行中（含念完後約 0.7 秒），語音指令自動忽略辨識結果**：避免喇叭念出的「鯨魚」「魚群」被麥克風當成指令。
- 本站不傳送任何文字；語音由瀏覽器合成，若瀏覽器選到雲端聲音，是否連網由瀏覽器與系統決定。
- 限制：可用的聲音因裝置而異（Windows / Android 可能沒有 `zh-TW` 聲音而退到別的中文聲音，甚至完全沒有；Linux Chromium 常常一個聲音都沒有，此時整段無聲，導覽照常）；iOS Safari 與部分行動瀏覽器要先點過頁面才會出聲，沒點過就靜默不念；英文模式下測站與河川名維持中文原文，英文聲音遇到可能跳過或亂念；缺值的「—」不會被唸成「無資料」。**真實語音合成的聲音與品質尚未實測**（內嵌預覽面板沒有測旁白），見〈驗證狀態〉。

**中斷與還原**：導覽前的參數、海況選項與播放倍速會被記下。碰螢幕、動旋鈕 / 滑桿，或按 `T` `H` `I` `?`、`←` `→` `P`、修飾鍵、`Tab` 以外的任何鍵（包含 `L` 與 Footer 的「系統事件」開關），導覽立即停止並還原成導覽前的海（`Esc` 或再按 `T` 也可停止）。語言切換、分享、分享星球、錄影、資訊、說明、聲音、匯出 LOG 不算中斷。若你是開始錄製、自己按播放或換海況，則只停導覽、不還原。你暫存的錄製會保留；導覽不會灌水展場角落的「演出 N 次」。

**先還原、再動作**：DOM 事件（點螢幕 / 按鍵）在 capture 階段先中止導覽；非 DOM 的輸入——MIDI 旋鈕與按鈕、語音指令、手機遙控（含走帶鍵）、手把、相機手勢、滾輪——也一律先呼叫 `activity.touch()`，它的同步掛鉤（`onActivity`）會在動作**之前**中止導覽並還原，所以你的第一個動作（清垃圾、安靜、● 錄製、▶ 播放、旋鈕）落在還原後的海上，不會被 100ms 後的還原蓋掉。分頁被隱藏 / 最小化 / 被完全蓋住時導覽中止並還原（背景時 `requestAnimationFrame` 停了，導覽卻照時間換站、字幕照換，會變成靜止的海配上會動的說明）。只按面板、關彈窗、看資料卡，或播完自己的序列，也算一次活動（30 秒後才會被導覽接手）；自動導覽開始時會收掉開著的資料卡。導覽自己的換站 / 播放 / 溢流不會讓手機或手把震動（你叫出的鯨魚 / 海豚 / 海龜 / 淨化照震）。

**循環**：手動開始的導覽播完一輪自動結束並還原；閒置自動啟動的會一直循環到有人操作。有彈窗（說明 / 裝置 / 多人）開著時不會自動開始；沒有海況資料時退回舊的色相輪播。

**隱私與權限**：只用已載入的開放資料，不需要任何權限、不上傳任何東西（旁白見上）。觀眾視窗會同步顯示導覽字幕（導覽只在主視窗執行，觀眾視窗不會自己啟動）。

---

## 點物件看資料出處與觸控筆

**點物件看資料出處**：輕點（位移 < 10 像素、按住 < 0.45 秒、單指、非雙擊的第二下）畫布**背景**上的物件，會在該物件旁彈出資料卡，每張卡片都附資料來源：

| 物件 | 卡片內容 |
|---|---|
| 測站星（188 座河川流量測站的背景星座） | 站名、河川、集水面積、現存或已廢、亮度說明 |
| 月亮 | 今日月出、中天（含仰角）、月沒、現在的方位與仰角、月相（潮汐海況則顯示月相與潮差） |
| 球外鳥群 | 流域、本月鳥種數（缺月標「內插」）、調查年範圍、推算群數 |

點空白處、按 `Esc`、按卡片右上 ×、或 6 秒沒操作會關閉（游標停在卡片上不會自動關）；按 `I` 關閉畫面資訊後不彈卡片；語言切換時卡片文字即時換（站名與河川名維持中文原文）；窄畫布（≤520px）卡片停靠在畫布下緣。
限制：星座或月亮淡出時、AR 實景時不能點；球體範圍內的輕點仍是「亮星爆發」，所以疊在球體範圍內的鳥群不會開卡；在星上快速雙擊時第一下會先開卡，切演出模式後卡片最多再停留 6 秒。觀眾視窗會顯示主視窗的卡片（位置以畫布相對座標鏡像，兩個視窗長寬比不同時圓環與物件可能有些微偏差）。

**觸控筆壓力與傾斜**（僅 `pointerType === 'pen'`：Apple Pencil、Surface Pen、Wacom；滑鼠與手指不會觸發）：

- 壓力：拖曳球體時，壓得越用力，浪勁（轉動慣量與浪湧增量）越大。
- 傾斜：筆按在畫布上時，筆身傾斜推動洋流方向 X / Y（筆立起、傾斜在 6 度內不動；筆懸空不按時不動；更新頻率不超過每秒 20 次）。傾斜是**絕對映射**，而自然握筆本身就會傾斜約 20–45 度，所以用筆時洋流會偏向握筆方向——展場若覺得太敏感，可用 `?pen=0` 關閉。
- iOS Safari 的 Apple Pencil 可能只提供 `altitudeAngle` / `azimuthAngle` 而沒有 `tiltX` / `tiltY`，程式有後備換算（見驗證狀態：尚未在真實 iPad 驗證）。
- 不使用任何權限、不蒐集資料；沒有「裝置」面板的開關。

---

## 調查空窗時間軸

資料卡的鳥群 / 魚群調查卡有「調查時間軸」：依真實年份等比例畫出每個調查年（小柱高度依物種數），沒有調查的年份畫成琥珀色斜線陰影並標「無調查 2007–2013」（例：曾文溪魚類 2004–2015 共 12 個日曆年，其中 2007–2013 沒有調查）。

- 按「▶ 播放調查年表」逐年推進，每年約 0.7 秒（曾文溪魚類 12 步約 8.4 秒）；空窗年以**前後調查年的線性內插**驅動海況，時間軸上的游標掃過並亮起目前年份，畫布上方的資料提示與 OUT 日誌都明講「無調查（內插）」與整段空窗區間。
- **內插值只是示意，不是觀測資料**；平均 / 基準只用真實調查年。
- 可及性：時間軸有 `role="img"` 與 `aria-label`（例如「調查年度 2004、2005、2006、2014、2015；2007 到 2013 無調查」）；版面全用百分比，≤400px 不會溢出；播放中游標由 `store.subscribe` 直接寫 DOM，不觸發 React 重繪。

---

## 新手導覽、系統事件監看與視角取景

三個讓第一次使用的人（尤其是手機）更容易上手的功能。

### 新手導覽

- **內容**：第一次進站自動出現，7 個小步驟——歡迎、轉動與觸碰、調整海、接上真實資料、資料導覽、更多功能、準備好了——每步一兩句話，並用聚光外框標出要看的地方（半透明遮罩 + 提示卡，卡片上有進度點、「第 n / 7 步」、上一步 / 下一步 / 略過）。目標在可捲動的面板裡時會先捲進視野，結束後還原原本的捲動位置；目標不存在或看不到時退回置中的卡片。
- **手機**：寬度 ≤ 520px 時提示卡改成貼近畫面下緣的 bottom sheet；目標在畫面下半（控制面板的滑桿、資料卡）時改貼上緣，避免蓋住聚光的目標。
- **操作**：桌面 `→` / `Enter` 下一步、`←` 上一步、`Esc` 略過；手機直接點卡片上的按鈕。導覽開著時焦點留在卡片內（`Tab` 只在按鈕間循環），背景的全域快速鍵不作用。
- **記住**：只有「完成」或「略過」才寫入瀏覽器（localStorage 的 `ixd2026.onboarded`，並與舊的 `ixd2026.seen` 成對寫入）；已經有 `ixd2026.seen` 的舊使用者不會被打擾；讀不到 localStorage（例如隱私模式）視為第一次，每次載入都會出現（同一分頁完成後改由 sessionStorage 記住）。首次到訪仍以今天真實的海開場。不上傳任何東西。
- **何時不出現**：`?onboard=0`；展場模式 `?kiosk`；資料導覽的每站連結 `?tourstop=`（收到連結的人要直接看到那一站）；手機遙控頁、觀眾視窗、診斷頁。`?onboard=1` 可強制顯示（遙控頁、觀眾視窗、診斷頁除外）。
- **重看**：「說明」→「重新看新手導覽」。
- **說明視窗**：長篇說明不再一進站就跳出，收進「說明」鈕（或 `?`）：一句導言 +「快速上手」5 條 + 6 個預設收合的主題區塊（操控方式、MIDI 與聲音、真實資料與導覽、畫面與實景、多人 / 分享與展場、語言與裝置）+「海洋生態」區塊。

### 系統事件監看（輸入輸出監看）

- **隱藏方式**：監看標題列右側的小箭頭、Footer 的「系統事件」開關（手機上顯示短字「事件」）、鍵盤 `L`。
- **預設與記憶**：桌面（寬度 > 820px）預設顯示、手機預設隱藏；你切換過就記住（localStorage 的 `ixd2026.monitor`，值為 `show` / `hide`）。沒有偏好、也沒有網址覆寫時，視窗跨過 820px 會自動跟著顯示 / 隱藏；一旦手動切換，就不再自動跟隨。`?log=1` / `?log=0` 只覆寫這一次、不寫偏好；優先序：網址 > 使用者偏好 > 依寬度預設。
- **隱藏時**：監看與它上方的分隔線都不渲染，畫布與右側面板往下延伸；日誌照常累積，「匯出LOG」與再次顯示都是完整的。Footer 改用一行小字（`aria-live="polite"`，更新有節流）顯示最新一則 OUT 事件。每次切換會在 OUT 日誌留一行「系統事件面板：顯示 / 隱藏」，所以匯出的 LOG 會多這幾行。
- 演出模式（`H` / `?kiosk`）本來就看不到監看與 Footer。桌面 Footer 的高度為配合新開關改為自動（最小 26px、可換行）。

### 視角自動取景

- **為什麼**：相機的垂直視角固定（45°），水平可視範圍等於垂直乘以畫布長寬比；手機直式全螢幕（例如 375×812，長寬比約 0.46）時，球會被左右切掉。
- **做法**：畫布比「球剛好貼滿」還窄時（長寬比約低於 0.95），相機沿原本的視線方向**拉遠**（只拉遠、不拉近）；更寬的畫布（桌面、橫式、接近正方形）倍率恆為 1，取景與過去逐位元相同。依公式計算，手機直式演出模式（375×812、390×844）相機距離約 12.9（原本 6.6，約 1.95 倍），平板直式 768×1024 約 1.24 倍。
- **行為**：縮放滑桿與雙指縮放照常可用（預設值一定看得到整顆球；在直式手機上仍可拉近拉遠）；視窗、螢幕方向、面板寬度改變時約 0.5–1 秒平滑調整；第一幀直接就位，不會先被切掉再縮回；WebXR 桌面放置時不動；`?fit=0` 關閉（除錯用）。全部在本機計算，不使用任何權限。
- **連帶調整**（只在拉遠時生效，桌面不變）：背景的河川測站星座在直式時改放到球的上方置中；霧的近 / 遠距離與背景星殼會跟著相機距離放大，否則水體被霧吃掉、星點會貼在鏡頭前變成大光斑。
- **限制**：寬螢幕上球殼在垂直方向約佔畫面 77%（維持現況，沒有為此改變）；直式手機上球的螢幕直徑較小（375 寬約 311px，約桌面 540px 的 58%），點物件選取的半徑等比例縮小；直式時測站星座可能與最上方的資料 HUD 條少量重疊；月亮圓盤在長寬比 < 0.31 的極端窄畫布上邊緣可能被切掉一點點（一般手機最窄約 0.39，不受影響）。

---

## 「裝置」面板

頂部「裝置」鈕開啟。面板集中放**選用**的裝置功能，每一節有自己的開關與狀態說明；常駐邏輯在 `src/services/*Service.jsx`，不開啟就不會用到相機 / 麥克風，也沒有額外的計算成本。

| 功能 | 預設 | 需要的權限 |
|---|---|---|
| 觀眾視窗（雙螢幕） | 關（按鈕開啟） | Chrome / Edge 自動放到外接螢幕時：「視窗管理」 |
| 相機手勢 | 關（需明確打開） | 相機 |
| 語音指令 | 關（需按「開始收音」；重新整理後一定是關） | 麥克風 |
| 觸覺回饋 | 觸控手機開、桌機關、系統要求「減少動態效果」時關 | 無 |
| 畫質 | **自動**（會依 FPS 自己升降級；可手動鎖定） | 無 |
| 裝置診斷 | 一個連結（按「開啟裝置診斷」開新分頁）+ 上次診斷的摘要 | 無（診斷頁裡的相機 / 麥克風等互動檢查才各自要求權限） |
| 維運 | 一律顯示，不需要開啟 | 無 |

> 注意：畫質的預設是「自動選擇」，也就是進站就會量測 FPS 並在畫面卡頓時自動降級（詳見〈畫質〉），不是「預設關閉」。

### 觀眾視窗（雙螢幕）

- **怎麼開**：「裝置」→「觀眾視窗（雙螢幕）」→「開啟觀眾視窗」；「關閉觀眾視窗」會請該視窗自己關閉。面板顯示連線中的觀眾視窗數與偵測到的螢幕數。
- **能做什麼**：筆電操作、投影機給觀眾看。開一個沒有控制介面、不出聲音的視窗（網址 `?audience=1`），全螢幕顯示同一片海，含資料看板、資料播放 HUD、導覽字幕與資料出處卡片。主視窗的參數、資料播放（倍速 / 循環）、鯨魚 / 海豚 / 海龜 / 淨化波、打擊墊效果、語系、資訊面板開關都即時同步。
- **權限與支援**：Chrome / Edge 在 HTTPS 或 localhost 下會詢問「視窗管理」權限（`getScreenDetails`），允許後視窗自動放到外接螢幕，在該視窗點一下即進入全螢幕。**Safari / Firefox、只有一個螢幕、拒絕權限、非安全來源**時退回開一般視窗，請手動拖到投影機畫面再點一下進入全螢幕。首次授權可能讓使用者手勢過期而被彈窗封鎖器擋下，面板會提示「請再按一次」。
- **螢幕不休眠**：觀眾視窗自己持有 Screen Wake Lock（系統層級：投影機與筆電螢幕都不會因為沒人碰鍵盤滑鼠而休眠，導覽跑很久也一樣）；回到前景會重新申請，不支援的瀏覽器靜默略過。
- **畫質**：投影機常是 4K / 高 DPR，是像素負擔最大的畫面，所以觀眾視窗**用自己的 FPS**自動調整畫質（它的畫質不寫共用偏好，不會和主視窗互相覆寫）；主視窗在「裝置 → 畫質」手動選一級時，已開著的觀眾視窗會跟著鎖定同一級（選回自動就改用自己的 FPS）。投影機若被限制在 30 Hz，自動模式會誤判成效能不足而降級——請在主視窗手動選一級。
- **隱私**：資料只在同一個瀏覽器的兩個視窗之間以 BroadcastChannel（`midisea-audience`）傳遞，不上傳；沒有觀眾視窗連著時主視窗不做任何額外工作。
- **限制**：觀眾視窗是「同參數、同事件」的**複本，不是像素級鏡像**——水母 / 魚群位置、垃圾物理、球體自轉與果凍壓凹不同步。兩個視窗必須同源（同一個網址）。同一瀏覽器開兩個控制台分頁時，觀眾視窗鎖定第一個回應的主視窗。主視窗分頁被切到背景時（瀏覽器降頻計時器）觀眾視窗可能短暫顯示「等待主視窗…」，回到前景後自動恢復。手動開啟的分頁（不是腳本開的）可能無法被「關閉觀眾視窗」關掉。

### 相機手勢

- **怎麼開**：「裝置」→「相機手勢」→「啟用相機手勢」。打開之後才會要求相機權限。
- **能做什麼**：用相機看你的手，不必碰螢幕。
  - **張手**：五指張開並停留約 0.5 秒，洋流速度、游動速度與垃圾數量會慢慢平靜下來（朝約 0.15 / 0.3 / 0.1 靠近；放手後不會彈回、其他輸入可立刻接手）。
  - **捏合**：拇指與食指捏在一起，召喚一隻鯨魚（每 3 秒最多一次，捏著不放不會重複）。
  - 其他手勢（握拳、比讚、比耶、手指併攏）一律忽略；手離開鏡頭超過 1 秒回到閒置。分類只用距離比值與夾角，左右手、鏡像前鏡頭、任意旋轉都適用。
- **畫面**：畫布右上角顯示「相機使用中」紅點（**永遠顯示**，不受 `I` 控制）與「手勢 · 偵測到：張手 / 捏合 / —」（跟著資訊面板開關）。
- **權限與隱私**：相機。影像只在這台裝置上處理，**不上傳、不錄下來**；關閉開關、離開頁面或切到背景就立刻停止相機（回到這一頁時自動重新開啟）。AR 實景開著時共用同一個相機串流，否則開一個隱藏的前鏡頭 640×480 `<video>`。
- **需要下載的東西**：第一次啟用會下載手部模型（約 8 MB，來自 Google 儲存空間）與 WebAssembly 運算程式（未壓縮約 11.7 MB，來自 jsDelivr CDN、傳輸時經壓縮；兩者由瀏覽器快取）。MediaPipe（`@mediapipe/tasks-vision`，版本釘死 1.0.1）只在開啟時動態載入，是獨立的 chunk，不進主 bundle。這兩個網路請求會讓 CDN 看到你的 IP，但不會傳送任何影像。Service Worker 不快取跨來源資源，所以**離線的第一次使用會失敗**（面板有明確訊息）。
- **支援與限制**：需要 HTTPS 或 localhost、`getUserMedia`、WebAssembly。偵測約 15 fps、跑在主執行緒（GPU delegate，失敗退回 CPU）；推論慢的裝置（舊手機 / iPad）會量每次推論花的時間並拉長偵測間隔（推論後至少休息 2 倍時間、上限 100ms，主執行緒佔用率壓在約 1/3 ~ 1/2），代價是手勢反應變慢；畫質自動降級不會減少推論成本，也不會暫停它。分頁進背景時暫停偵測並放掉自己開的相機。AR 用後鏡頭、手勢用前鏡頭，部分 Android 裝置無法同時開兩個相機。**分類門檻是在合成手部模型上調校的，未用真實相機調過**（見〈驗證狀態〉）。

### 語音指令

- **怎麼開**：「裝置」→「語音指令」→「開始收音」，瀏覽器詢問時允許麥克風。
- **指令**：說出「鯨魚、海豚、海龜、大浪、亮星、淨化、清垃圾、安靜、快一點、停」就會觸發對應效果，可連著說多個（例如「鯨魚 大浪」）；同一指令 1.5 秒冷卻。畫布下方短暫顯示「聽到：鯨魚」。英文對應：whale、dolphin、turtle、big wave（wave）、sparkle（star）、purify、clean、quiet（calm）、faster、stop。
  - 「安靜」讓洋流、游速、垃圾一起朝平靜值靠近一段（連說兩次更平靜）；「快一點」讓洋流與游速朝高值移動；「停」停止錄製 / 播放。
  - 中文詞表刻意寬鬆（含常見誤辨與簡體），所以會有誤觸發，例如「進化」也算淨化、「金魚」也算鯨魚、英文單字 wave / star 就會觸發。
- **辨識語言**跟著介面語言（中文 `zh-TW` / English `en-US`）切換，一次只辨識一種；想說另一種語言請先切換介面語言。
- **權限與隱私**：麥克風。Chrome / Edge 的語音辨識會把麥克風聲音送到雲端服務（Google / Microsoft）辨識，**需要網路**；Safari 依系統設定處理。本站**不錄音、不保存、不上傳音訊**。只有按下「開始收音」才會建立辨識器並要求權限；收音中畫布右上角**永遠顯示**「收音中」徽章（按 ✕ 立即停止）；分頁進背景自動暫停、切回自動接續。與工具列「麥克風」（吹氣＝風）是各自獨立的功能，可同時開（少數手機同時收音時其中一項可能失靈）。
- **支援**：Chrome、Edge、Safari（Web Speech `SpeechRecognition` / `webkitSpeechRecognition`）；**Firefox 不支援**（開關停用並說明）。
- **限制**：Android Chrome 每次重啟辨識可能有系統提示音；iOS Safari 不支援真正的 continuous，會頻繁結束重啟、可能反覆詢問權限；Brave / Electron 常回 network 錯誤（重試 5 次後自動停用）。「中文 + English 同時」交替辨識沒有做。資料導覽的語音旁白進行中（含結束後約 0.7 秒），語音指令會自動忽略辨識結果，避免喇叭念出的「鯨魚」被麥克風當成指令。

### 觸覺回饋

- **怎麼開**：「裝置」→「觸覺回饋」：開關、強度（弱 / 中 / 強），「測試觸感」會依序播放鯨魚（長而低）、水滴（短而輕）、淨化波（漸強）、海豚（兩下輕點）、海龜、錄製開始。
- **事件**：鯨魚、海豚、海龜、淨化波、錄製開始 / 停止、播放開始 / 停止、溢流（海水高度 > 97% 時先一串細碎輕震，再以固定節奏滴水，越滿越快）。有冷卻與全域每秒上限，連續快速點鈕不會變成持續嗡嗡。
- **裝置**：手機（Android Chrome 等）用震動馬達（`navigator.vibrate`）；手把用雙震動馬達（Gamepad `vibrationActuator`，dual-rumble，僅 Chrome / Edge；Chrome 需要先按過手把按鍵才偵測得到）。
- **預設**：觸控手機開、桌機關（接手把後可手動開）；系統開啟「減少動態效果」時預設關。iOS Safari 沒有震動 API，會靜默略過，可改用手把。
- **隱私**：設定（開關與強度）只存在這台裝置的 localStorage（`ixd2026.haptics`），不傳送任何資料。
- **限制**：手機遙控頁（掃 QR 的那支手機）不掛載服務，所以遙控手機本身沒有觸覺回饋；點擊亮星尚未接觸覺；溢流水滴以固定節奏模擬，不與聲音同步；`navigator.vibrate` 在桌機 Chrome 也存在但沒有馬達，面板的「支援」只代表 API 存在。

### 畫質

- **怎麼用**：「裝置」→「畫質」→ 自動選擇 / 高畫質 / 中畫質 / 低畫質；顯示目前等級、目前 FPS（每秒更新）與上次自動降級的原因。
- **自動模式**（預設）：連續 3 秒平均低於 40 FPS 降一級；連續 8 秒高於 55 FPS 升回一級；換級之間有最短停留（10 秒）與冷卻，升級後又被降回去會加倍升級冷卻，不會來回抖動。拖曳、錄製 / 錄影或分頁在背景時暫停調整。手動選一級就鎖定，不再自動升降。
- **各等級**：

  | 等級 | 解析度上限 | 生物與粒子 | 其他 |
  |---|---|---|---|
  | 高（現況） | 2 倍像素比 | 100% | 全部效果 |
  | 中 | 1.5 倍 | 約 70% | — |
  | 低 | 1 倍 | 約 50% | 關閉流星；背景模糊不套用，背景清澈改用簡化暗化 |

- **隱私**：只量測畫面更新速度；偏好（模式與最後等級，`ixd2026.quality`）只存在這台裝置。自動模式會記住最後的等級，下次載入從該級起跳。`?quality=` 只是本次覆寫，`?fps=1` 顯示 FPS。
- **限制**：量到的是 rAF 節奏而不是 GPU 時間；iOS 低耗電模式或 30Hz 螢幕會把 rAF 限到 30 fps，自動模式會誤判為效能不足而降到低——請手動鎖定等級。降級會讓 canvas 重新配置大小，分享圖 / 錄影的解析度會跟著目前等級（錄影期間不會換級）。

### AR 桌面（WebXR）

- **怎麼開**：只在「瀏覽器有 WebXR 且 `isSessionSupported('immersive-ar')` 為真」的裝置出現（實務上是 Android Chrome + ARCore）。「裝置」→「AR 桌面」→「在桌面上放置」→ 允許相機權限。用鏡頭掃桌面 / 地板，出現準星環後點一下，就把直徑約 28 公分的海洋球放在那裡；走動時球固定不動，可繞著看各個角度。
- **控制**：畫面下方的 DOM overlay 有海水高度 / 洋流速度 / 海水清澈三個滑桿、鯨魚 / 海豚 / 海龜按鈕、「重新放置」與「退出 AR」（走同一條 `input()` 路徑，所以可錄製）。追蹤遺失時會提示「移動手機，找一個平面」。
- **權限與隱私**：相機只在你按下按鈕後才啟用；影像由瀏覽器的 WebXR 處理，本站看不到也不上傳。「實景」開著時這個按鈕會停用（兩者搶相機）。
- **零成本降級**：所有 XR 程式碼都是獨立的 lazy chunk（`xr` / `XrRuntime` / `XrOverlay` 各約 2–3 kB gzip）。沒有 `navigator.xr` 的裝置（iOS Safari、多數桌機）完全不下載，該節也不出現；退出、被系統中斷或發生錯誤都會回到一般 2D 畫面。
- **驗證狀態**：**沒有在真機驗證**（開發環境沒有 XR 裝置，內嵌瀏覽器也不支援）。狀態機與放置數學有 35 項單元測試（假 session）；渲染層以假 XR manager 做過煙霧測試；一般 2D 畫面在真實瀏覽器確認與改動前一致。hit-test 準確度、DOM overlay、加法混合線稿疊在 passthrough 上的可見度與效能都需要實機確認。
- **限制**：只接受水平面（法線 y ≥ 0.75）；沒有使用 XR anchors，ARCore 長時間追蹤修正時球可能微幅漂移；沒有 DOM overlay 的環境（如 Quest Browser 的 immersive-ar）仍可放置，但看不到滑桿與退出鈕。
- **實機驗證步驟**（有 Android 手機的人請照做並回報）：① 用 HTTPS（或 USB 除錯 + `adb reverse tcp:5173 tcp:5173` 開 localhost）；② 「裝置」面板最下方出現「AR 桌面」，iPhone Safari 與沒有 AR 的桌機不會出現；③ 進入後看到相機畫面、無深藍背景與星空，掃桌面出現青色環；④ 點一下放置，繞一圈確認固定不動、粒子大小協調；⑤ 拖 overlay 三個滑桿、點三種生物；⑥ 遮住鏡頭確認追蹤遺失提示與恢復；⑦ 退出後畫面、背景層、背景模糊 / 清澈都正常；⑧ 英文語系重複一次。

### 裝置診斷

- **怎麼開**：「裝置」→「裝置診斷」→「開啟裝置診斷」（開新分頁，網址 `?diagnostics=1`）。面板顯示「上次診斷：時間，通過 N 項、失敗 M 項」；診斷頁在另一個分頁更新時，面板會即時更新。
- **隱私**：這裡只存一份摘要（時間與各狀態的項數；localStorage 的 `ixd2026.diag`），不含任何裝置資訊。診斷頁本身見〈裝置診斷頁〉。

### 維運

- **內容**：版本（build id，開發版顯示 `dev`）、載入時間、海況資料時間、資料檢查與版本檢查的最近結果、崩潰紀錄（總筆數與近 10 分鐘造成重新載入的次數，可展開最近幾筆）、目前啟用的防呆（渲染看門狗、WebGL 復原、自動更新版本、資料更新、每日重新載入）。
- **按鈕**：立即重新載入、立即檢查更新、清除崩潰紀錄。
- **隱私**：紀錄只存在這台裝置，不會傳送任何資料。一般使用不需要動這一節；各項防呆的行為見〈展場防呆與維運〉。

---

## 裝置診斷頁

到現場、或換到一台沒用過的裝置之前，用實際的硬體逐項確認相機、麥克風、語音、MIDI、手把、震動、螢幕、全螢幕……能不能用。它是「真機驗證日」的主要工具（見〈驗證狀態〉）。

- **怎麼開**：網址 `?diagnostics=1`（例如 `https://midisea.shyetech.com/?diagnostics=1`；開發時 `http://localhost:5173/?diagnostics=1`），或「裝置」面板 →「開啟裝置診斷」（開新分頁）。手機與桌機都能用。它是獨立頁面，不載入 3D 場景與主畫面，頁首有自己的 `EN` / `中文` 切換鈕（切換不必重跑檢查）。
- **快速檢查（25 項）**：按「執行所有快速檢查」一鍵跑完，約 3–4 秒；不需要任何權限，也不會啟動相機或麥克風。分四組：
  - **環境資訊**：瀏覽器與平台、螢幕與視窗大小、安全環境（https）、語言與時區、網路狀態、處理器與記憶體、儲存空間估計。
  - **圖形與效能**：WebGL（繪圖器與最大貼圖）、2 秒畫面幀率取樣（平均與最低）。
  - **瀏覽器功能支援**：localStorage 讀寫、BroadcastChannel 往返、Service Worker、Web Share 分享圖片、剪貼簿寫入、全螢幕 API、Screen Wake Lock（請求後立刻釋放）、MediaRecorder 錄影格式。
  - **裝置 API 是否存在**：Web MIDI、Web Bluetooth、WebXR AR（只查支援度，不開 session）、語音辨識、Gamepad、震動、PointerEvent 與觸控點數、`getScreenDetails`。
- **互動檢查（12 項）**：每項要按自己的按鈕才會啟動。相機（預覽 + 實測解析度與 fps）、麥克風（3 秒音量條）、語音辨識（5 秒，顯示辨識文字與語言）、Web MIDI 裝置清單、手把（即時按鍵與搖桿）、手把震動、觸控筆與觸控畫板、手機震動（震完回答有沒有震到）、方向與動作感測器（iPhone 會先詢問權限）、螢幕清單（`getScreenDetails`）、彈出視窗、全螢幕進入與離開。相機畫面、手機震動與全螢幕可回答「正常 / 不正常」（「不正常」會把該項變成失敗）；其中震動必須回答才算通過，其餘可不答。
- **結果狀態**：通過 / 失敗 / 不支援 / 待操作 / 略過 / 資訊。徽章同時用文字、顏色與形狀表示，不只靠顏色。
- **報告**：按「複製報告」得到 Markdown 表格（37 列，沒測的標「尚未測」）加一段 JSON；剪貼簿被瀏覽器擋下時會改顯示可手動全選的文字框。也可按「下載 JSON」得到 `midisea-diagnostics-YYYYMMDD-HHMMSS.json`。
- **隱私**：所有檢查都在你這台裝置的瀏覽器裡執行，**不上傳任何東西**。報告不含個人資料、IP、影像或音訊；不收相機 / 麥克風 / 螢幕的裝置名稱；**但 Web MIDI 埠名稱與手把的型號字串會照實列出**（判讀有沒有偵測到 KORG 控制器要用；藍牙 / 網路 MIDI 埠與手把有時以擁有者命名，貼出報告前請看一眼）；語音辨識只記字數、不記內容；網址去掉 `#hash`。相機、麥克風與語音只在你按下按鈕後才啟動，按「停止」、離開頁面或分頁被隱藏時全部釋放。注意 Chrome 的語音辨識本身會把聲音送到雲端服務（與〈語音指令〉相同）。「裝置」面板只讀寫一份摘要（`ixd2026.diag`）。
- **判讀時請注意**：
  - 摘要的「通過」包含純資訊項目（UA、螢幕、語言…）；離線或非 https 會被判為失敗（這是刻意的展場檢查）；Service Worker 在開發模式（不註冊）顯示「待操作」。
  - 幀率取樣量的是空白診斷頁的 `requestAnimationFrame`，反映螢幕更新率與合成器狀態，**不代表** 3D 場景負載下的 FPS；門檻 45 FPS 是經驗值；分頁在背景時該項會略過或逾時失敗。
  - 有些項目受瀏覽器限制：例如 iPhone 沒有震動、網頁全螢幕與 WebXR，多數功能需要 https。
- **驗證狀態**：診斷邏輯有 57 項單元測試（假物件會檢查 `this` 綁定；故意把原生呼叫改成脫離物件的呼叫後測試會轉紅）。在真實瀏覽器（內嵌預覽面板，Chrome 引擎，桌面與手機模擬）跑過快速檢查：19 項通過、1 項失敗（剪貼簿：權限被內嵌面板拒絕）、2 項不支援（Web Share 與 WebXR），其餘為待操作或略過；「裝置」面板的診斷與維運兩節也看過。**互動檢查（相機、麥克風、語音、震動、全螢幕、螢幕管理）沒有實測**（內嵌面板擋相機與麥克風）；剪貼簿在一般瀏覽器的成功路徑（只驗證了失敗提示）、iOS Safari 各項、Android 真機都沒有驗證——這些正是真機驗證日要做的事。

---

## 展場防呆與維運

整天不關機的展場沒有工程師在旁邊，所以畫面出錯、顯示引擎中斷、渲染卡死或有新版時，要能自己復原。邏輯在 `src/ErrorBoundary.jsx`、`src/lib/resilience.js` 與 `src/services/ResilienceService.jsx`；狀態與工具在「裝置 → 維運」。

| 防呆 | 行為 | 什麼時候啟用 |
|---|---|---|
| 錯誤邊界（ErrorBoundary） | 畫面 render 出錯時，換成「發生錯誤，N 秒後自動重新載入」、「立即重新載入」鈕與可展開的錯誤摘要，倒數到 0 自動重載。**退避 5 → 15 → 60 秒**：第 1 次 5 秒；1 分鐘內第 2 次 15 秒、第 3 次起 60 秒。**10 分鐘內累積 5 次即熔斷**：停止自動重載，改顯示「發生錯誤，請人工處理」與手動重載鈕，避免無限重載風暴。同一瞬間（200 毫秒內）的多筆致命事件併成一次事故，一次崩潰不會被算成兩次。`window` 的 `error` / `unhandledrejection` 只記錄、不重載 | 一律啟用（包在最外層，主畫面、觀眾視窗、手機遙控頁、診斷頁都有） |
| WebGL 遺失復原 | 顯示引擎（WebGL context）遺失時，畫面上方顯示「顯示引擎暫時中斷，嘗試恢復中…」；**4 秒**內沒恢復就自動重載，有恢復則顯示「顯示引擎已恢復」幾秒後消失。分頁在背景時的遺失不會在背景重載，回到前景再給 4 秒 | 主畫面與觀眾視窗一律啟用 |
| 渲染看門狗 | 分頁可見卻**連續 10 秒沒有任何動畫幀**（`requestAnimationFrame`）時判定卡死，自動重載；需連續 2 次檢查都判定才動作，分頁切到背景時不判定 | 展場模式 `?kiosk`、**觀眾視窗**（投影機整天開著，卡死沒人發現）或 `?watchdog=1`；`?watchdog=0` 一律關；一般模式預設關（避免背景 / 省電情境被誤重載） |
| 版本檢查 | 建置時輸出 `dist/version.json`（`{ id, builtAt }`；id 是**程式碼內容**的雜湊——`src/`、`public/`（不含 `public/data/`）、`index.html`、`package.json` / `package-lock.json`、`vite.config.js`，不含測試與文件；同一份程式碼不論建置幾次 id 都相同，也編進程式），頁面定期以 `fetch('/version.json', { cache: 'no-store' })` 比對，不同就是有新版。**每 3 小時只換 `ocean.json` 的資料部署不會改 id**，所以不會讓任何視窗重載（資料由下一列就地更新）。有新版時，**只在閒置時**（無人操作 ≥ 60 秒，且沒有錄製 / 播放 / 導覽 / 彈窗）自動重新載入；忙碌就每 15 秒再看一次。離線或失敗時靜默略過（service worker 不攔截 `version.json`，離線時不會讀到舊快取而反覆重載） | 正式建置一律檢查：每 10 分鐘（展場 5 分鐘）；開發版（dev）不檢查；`?autoupdate=0` 只記錄、不重載 |
| 資料自動更新 | 定期重抓 `data/ocean.json`；`fetchedAt` 較新時**只換資料，不動你目前選的海況與任何參數**（閒置時才套用；連動中的鳥 / 魚數量不會因新資料自動重算，要等你下次按「套用」）。套用成功時 OUT 日誌出現「資料已更新」一行 | 主畫面：展場每 30 分鐘、一般每 3 小時（僅頁面可見時）。**觀眾視窗也做**：每 30 分鐘、不限頁面可見，就地換資料、不重載（投影機不會為了純資料的部署掉出全螢幕，也不會與主視窗顯示不同的數值） |
| 每日重載 | 網址加 `?reload=3`，每天本地時間 03:00 之後、閒置時重載一次；重載後重新計算成「明天」，不會連環重載 | `?reload=HH`（0–23） |

**閒置的定義**：無人操作至少 60 秒，且沒有錄製、資料播放、手動導覽或彈窗。展場模式（`?kiosk`）或「閒置自動啟動」的導覽本來就是沒人時的常態（自動導覽會無限循環），所以它們進行中不算忙；錄製、彈窗與 60 秒內有人操作仍然算忙。**導覽員操作（`←` `→` `P`、點進度點 / 導覽卡按鈕）也算有人操作**——這些操作刻意不算「操作海」（不會中止導覽），但導覽員正在講解時，60 秒內不會被版本更新或每日重載打斷。**手機的 WebXR 桌面 AR 進行中一律算忙**（重載會直接結束 AR，展場模式也一樣）。**觀眾視窗在全螢幕（投影機）時，新版的重載會延後**到退出全螢幕為止（重載會退出全螢幕，要有人走到投影機前點一下才能回去；資料就地更新不受影響，WebGL 遺失與看門狗這類卡死的重載也照常進行）。

**崩潰紀錄**：環狀保留最近 20 筆（時間、訊息、stack 前 300 字、build id、網址旗標摘要），同一次載入內重複的相同錯誤合併成一筆。新版 / 每日重載也會記一筆資訊性事件，不計入崩潰次數與熔斷（熔斷只算會造成重載的畫面錯誤、WebGL 失效與看門狗）。紀錄只存在這台裝置的 localStorage（`ixd2026.crashes`），**不會上傳**；可在「裝置 → 維運」查看與清除。

**建議的展場設定**：`?kiosk=1&reload=3`（可再加 `&lang=en&hud=0`），主機端另開觀眾視窗到投影機（它預設就有看門狗）。若看門狗在你的環境誤判，加 `?watchdog=0` 關掉。

**驗證狀態（誠實聲明）**：
- 防呆邏輯有 84 項單元測試（假環境、假計時器；含 `this` 綁定回歸）、6 項 service worker 測試（`scripts/sw.test.mjs`：用 `vm` 載入真實的 `public/sw.js`）與 13 項建置設定測試（其中 1 項要設 `MIDISEA_VERIFY_DIST=1` 才會跑的 dist 驗證，預設略過）。
- 在真實瀏覽器（內嵌預覽面板）驗證過：**錯誤邊界**（人為讓元件丟錯 → 倒數 → 自動重載後恢復）與「裝置 → 維運」面板。
- **沒有**在真實環境驗證：WebGL 遺失復原、渲染看門狗（包含它在真實投影機環境的誤判率；10 秒門檻是經驗值）、資料自動更新、每日重載，以及**正式部署後 `dist/version.json` 的版本檢查與自動重新載入**（要上線後才能驗）。
- 新版偵測可能比實際部署晚最多約一個 CDN 快取週期（`/version.json` 會經過 Service Worker 與部署 CDN 的快取）；離線時 Service Worker 可能回舊的 `version.json`，結果只是「已是最新」。

---

## 瀏覽器 / 裝置支援矩陣

**讀表須知**：本表依各功能實際使用的瀏覽器 API 與其公開的相容性推得。本專案主要在**桌面 Chrome / Edge**（含內嵌瀏覽器面板）開發與驗證，Safari、Firefox、iOS、Android 各欄**沒有逐項實機驗證**，「未驗證」就是字面意思。

圖例：**支援**＝該瀏覽器提供所需 API，功能預期可用；**條件**＝需要額外條件（HTTPS、權限、特定平台）；**降級**＝沒有該 API，功能退回替代方案；**不支援**；**未驗證**＝理論上可用，但沒有人實測過。

| 功能（所需 API） | Chrome | Edge | Safari | Firefox |
|---|---|---|---|---|
| 3D 海洋球與基本操作（WebGL、Pointer Events、Web Audio） | 支援 | 支援 | 未驗證（SPEC 目標為 16.4+，次要） | 未驗證（SPEC 目標為次要） |
| MIDI 控制器（Web MIDI；HTTPS 或 localhost） | 支援 | 支援 | 不支援（macOS 與 iOS 皆無） | 條件（108+ 需另外授權；未驗證） |
| 藍牙 MIDI（Web Bluetooth） | 支援（桌面、Android） | 支援 | 不支援 | 不支援 |
| 多人合奏 / 手機遙控（WebRTC；手機感測器需 DeviceOrientation / DeviceMotion） | 支援 | 支援 | 支援（感測器首次觸碰請求權限） | 未驗證 |
| AR 實景（`getUserMedia`；HTTPS） | 條件（HTTPS + 相機權限） | 條件 | 條件 | 條件 |
| 錄影（MediaRecorder + `canvas.captureStream`） | 支援（MP4 或 WebM） | 支援 | 支援（MP4） | 支援（WebM） |
| 分享星球（Web Share 檔案；否則下載 PNG + 複製文案） | 支援 | 支援 | 支援（桌機可能拒絕剪貼簿，靜默略過） | 降級（下載 PNG） |
| 觀眾視窗同步（BroadcastChannel） | 支援 | 支援 | 支援 | 支援 |
| 觀眾視窗自動放到外接螢幕（`getScreenDetails` + 視窗管理權限） | 條件（HTTPS / localhost + 權限） | 條件 | 降級（一般視窗，手動拖到投影機） | 降級（同左） |
| 相機手勢（`getUserMedia` + WebAssembly + MediaPipe） | 條件（HTTPS + 相機權限；未實機） | 條件（未實機） | 未驗證 | 未驗證 |
| 語音指令（Web Speech `SpeechRecognition`） | 條件（麥克風權限 + 網路；雲端辨識） | 條件（同左） | 條件（`webkitSpeechRecognition`，依系統設定；未實機） | 不支援 |
| 觸覺：手機震動（Vibration API） | 支援（僅 Android；桌機沒有馬達） | 支援（僅 Android） | 不支援（iOS 無此 API，靜默略過） | 未驗證 |
| 觸覺：手把震動（Gamepad `vibrationActuator`） | 支援（先按過手把按鍵才偵測到；未實機） | 支援（未實機） | 不支援 | 不支援 |
| 觸控筆壓力與傾斜（Pointer Events） | 支援（未實機） | 支援（未實機） | 條件（Apple Pencil 可能只有 `altitudeAngle` / `azimuthAngle`，有後備換算；未實機） | 未驗證 |
| 自動畫質（rAF 量測） | 支援 | 支援 | 支援（低耗電模式會誤判，見〈畫質〉） | 支援 |
| AR 桌面（WebXR `immersive-ar` + hit-test） | 條件（僅 Android；需 ARCore、HTTPS、相機權限；**未實機**） | 條件（僅 Android；未驗證） | 不支援（iOS Safari 沒有 WebXR AR，請用「實景」） | 不支援 |
| 裝置診斷頁 `?diagnostics=1`（各項 API 偵測；互動檢查另需權限） | 支援（快速檢查在內嵌預覽面板實測；互動檢查未實測） | 未驗證 | 條件（iPhone 沒有震動、網頁全螢幕與 WebXR，診斷頁會顯示不支援；**未實機**） | 未驗證 |
| 導覽語音旁白（Web Speech `speechSynthesis`，選用） | 條件（可用聲音因系統而異，可能沒有中文聲音；**聲音未實測**） | 條件（同左；未驗證） | 條件（iOS 要先點過頁面才會出聲；**未實機**） | 未驗證 |
| 展場防呆（錯誤邊界、WebGL 遺失復原、渲染看門狗、版本檢查、資料更新、每日重載） | 支援（錯誤邊界在內嵌預覽面板實測；其餘只有單元測試，版本檢查要上線後才能驗） | 未驗證 | 未驗證 | 未驗證 |
| 新手導覽、系統事件監看、視角自動取景（DOM、localStorage、純數學） | 支援（桌面與手機模擬實測） | 未驗證 | 未驗證 | 未驗證 |

**行動裝置備註**

- **iPad / iPhone（所有 iOS 瀏覽器）**：沒有 Web MIDI、Web Bluetooth 與震動 API——請用「多人」掃 QR 把手機當遙控器（WebRTC，不需藍牙）。傾斜 / 搖晃感測首次觸碰會請求權限。導覽旁白要先點過頁面才會出聲；診斷頁的震動、網頁全螢幕與 WebXR 項目會顯示不支援。以上都尚未在 iOS 真機驗證。
- **Android Chrome**：支援手機震動與 Web Bluetooth；AR 實景用後鏡頭、相機手勢用前鏡頭，部分機型無法同時開兩個相機串流，可能讓其中一個失敗。
- **舊手機 / iPad**：相機手勢的推論跑在主執行緒，可能拖慢畫面；建議手動鎖定「低畫質」。

---

## 驗證狀態（誠實聲明）

下面列出「做過什麼驗證」與「還沒驗證什麼」，給接手者與評審參考：**在展場或簡報前，請務必用真實裝置把「尚未驗證」的項目各跑一遍**（建議流程見本節最後的〈真機驗證日〉）。

### 本輪（第 4 輪）新增功能

本輪的功能由統籌者在**真實瀏覽器**（內嵌預覽面板，Chrome 引擎；桌面 1280×800，與手機 375×812 直向、812×375 橫向的模擬）實際操作過下列「已驗證」的項目。**沒有**在任何真機（iPhone、iPad、Android 手機、外接投影機）或完整的 Safari / Firefox / Edge 上驗證。

| 功能 | 已做的驗證 | 尚未驗證 |
|---|---|---|
| 裝置診斷頁 | 57 項單元測試；真實瀏覽器（桌面與手機模擬）跑快速檢查：19 通過 / 1 失敗（剪貼簿：權限被內嵌面板拒絕）/ 2 不支援（Web Share、WebXR）；「裝置」面板的診斷與維運兩節 | **互動檢查**（相機、麥克風、語音、震動、全螢幕、螢幕管理；內嵌面板擋相機與麥克風）；剪貼簿在一般瀏覽器的成功路徑（只驗證了失敗提示）；iOS Safari 各項；WebXR |
| 空氣品質 | 36 項測試（`scripts/gov/air.test.mjs` 20 + `src/lib/air.test.mjs` 16，含對真實 `ocean.json` 的檢查）；真實瀏覽器：選項、資料卡、「模型資料」標示、CC BY 標示、播放 | GitHub Actions 排程上的長期成功率（抓取腳本已在本機以真實網路跑過，排程上的表現要上線後才看得到）；與環境部測站實測值的差異（模型資料本來就會有落差） |
| 展場防呆 | 84 項單元測試（假環境、假計時器）+ 6 項 service worker 測試 + 13 項建置設定測試；真實瀏覽器：**錯誤邊界**（人為讓元件丟錯 → 倒數 → 自動重載後恢復） | WebGL 遺失復原、渲染看門狗（含它在真實投影機環境的誤判率）、資料自動更新、每日重載；**正式部署後 `dist/version.json` 的版本檢查與自動重載**（上線後才能驗） |
| 系統事件監看可隱藏 | 18 項測試；真實瀏覽器：`L` 鍵、Footer 開關、偏好記憶、手機預設隱藏 | 各種真機寬度（含 iPad 橫向）的 Footer 版面 |
| 新手導覽 | 49 項測試（含說明視窗結構檢查）；真實瀏覽器：7 步（手機與桌面的聚光與卡片位置）、完成後記住、`?tourstop=` 時不出現 | iOS Safari（含網址列收合）與其他真機的版面 |
| 視角自動取景 | 28 項測試（相機數學與狀態機，並以 three 的 `PerspectiveCamera` 實際投影驗證）；真實瀏覽器：手機直式演出模式看得到整顆球、橫向不變 | 真機；轉向時的實際手感；直式較小的球上點選物件是否仍容易點中 |
| 導覽員控制（含空氣品質站與每站連結） | 157 項測試（`tour` 84 + `tourCore` 55 + `tourLink` 18）；真實瀏覽器：`←` `→` `P`、進度點點擊、暫停時凍結播放與計時、暫停中跳站、繼續、`Esc` 還原速度與海況、深連結啟動且只啟動一次、手機直向與橫向的字幕卡 | 複製連結在一般瀏覽器的成功路徑（剪貼簿）；多數 CSS 在各種真機的版面 |
| 語音旁白 | 45 項旁白測試 + 3 項回音防護測試（假 `speechSynthesis`、假計時器；會檢查 `this` 綁定） | **真實語音合成的聲音與品質**（內嵌面板沒測旁白）；iOS Safari 的旁白；各裝置可用聲音的差異 |
| 溢流水滴排程 | 3 項測試（時間單調遞增，跑一萬次隨機序列不倒退） | 高更新率螢幕上的實際聲音 |

另外，下列項目本輪**完全沒有**驗證：WebXR（AR 桌面）、看門狗在真實投影機環境的誤判率、多數 CSS 在各種真機的版面。

### 先前新增的裝置與感測功能

這些功能寫程式的人手邊沒有對應的真實硬體。

| 功能 | 已做的驗證 | 尚未在真實硬體 / 環境驗證 |
|---|---|---|
| 相機手勢 | 56 項單元測試（分類、遲滯、冷卻、平滑、鏡像與左右手、丟手回閒置、以假相機 / 假 landmark 測的執行期生命週期）；設定區塊以伺服器端渲染檢查中英文 | 真實相機 + 真實 MediaPipe 輸出。**分類門檻（`THRESH`，`src/lib/gestures.js`）是在合成的手部模型上調校的**，實機很可能需要調整；GPU / CPU delegate 的實際效能 |
| 語音辨識 | 79 項單元測試（假辨識器、指令比對含誤辨與簡體、去重冷卻、重啟退避、語言切換）；UI 只做過伺服器端渲染 | 真實麥克風 + Web Speech：Chrome / Edge 雲端辨識、Safari；Android 的累加式逐字稿（只有單元測試）；iOS Safari 的 continuous 行為；詞表寬鬆度造成的誤觸發 |
| 雙螢幕（`getScreenDetails`） | 46 項單元測試（假 BroadcastChannel 的協定、節流、斷線偵測、快照自我修復、選螢幕邏輯）；同源 BroadcastChannel 可在單機兩個分頁模擬 | 真實外接螢幕 / 投影機；「視窗管理」權限流程；權限提示造成的手勢過期與彈窗封鎖；全螢幕行為 |
| 觸控筆 | 17 項單元測試（壓力 → 浪勁、傾斜 → 洋流、iOS 角度後備換算）＋ 39 項資料出處測試；選取投影數學在 Node 以 three 與真實 `ocean.json` 驗證（188 座測站都落在畫布內，自點命中 181 個，其餘 7 個座標與別站完全重合）；可用合成 `PointerEvent`（`pointerType: 'pen'`）模擬 | 真實 Apple Pencil / Surface Pen / Wacom；iOS 是否真的只給 `altitudeAngle`；自然握筆的傾斜是否過敏 |
| 手把震動 | 42 項單元測試（假 gamepad / 假 `vibrationActuator`、節奏表、強度縮放、冷卻與全域上限） | 真實手把（Xbox / PlayStation）的 dual-rumble 觸感；Firefox / Safari 的偵測行為 |
| Android 震動 | 同上（假 `navigator.vibrate`） | Android Chrome 真機的實際觸感與強弱區分（馬達只能控制長短，無法控制強弱） |

其他只在單元測試 / 伺服器端渲染 / 離線推算驗證、未做跨裝置實機驗收的項目：資料導覽字幕與卡片的 CSS 版面（含手機與演出模式寬度）、調查時間軸的視覺（斜線陰影對比、標籤位置、≤400px 版面）、資料出處卡片的位置與夾取、畫質自動降級（在真實低階裝置上）、畫質低等級的暗化與高等級的色差、海色校正的最終觀感（數值為離線推算並已含 ACES 色調映射）。既有功能（MIDI、多人合奏、AR 等）的驗證狀態不在本節評估。

**先前功能的實機驗證清單（接手者）**

1. **相機手勢**：Chrome 開「裝置 → 相機手勢」→ 允許相機 → 面板顯示「使用中」，畫布右上「相機使用中」紅點；五指張開約 0.5 秒 → 洋流速度 / 游動速度 / 垃圾漸降；拇指食指捏合 → 剛好一隻鯨魚，3 秒內不重複；握拳 / 比讚 / 比耶 → 無反應；關閉後相機燈熄滅。太難或太容易觸發就調 `THRESH`（`spreadEnter`、`fingerReachEnter`、`thumbAwayEnter`、`pinchEnter`）。
2. **語音**：Chrome 開「語音指令」→「開始收音」→ 允許麥克風 → 依序說鯨魚、海豚、海龜、大浪、亮星、淨化、清垃圾，畫布下方應顯示「聽到：…」；說「安靜」參數下降、「停」停止播放；切到 English 說 whale / big wave / quiet；封鎖麥克風權限後開啟，應出現權限提示且其他功能不受影響。
3. **雙螢幕**：Chrome / Edge 接外接螢幕（HTTPS 或 localhost）→「開啟觀眾視窗」→ 允許「視窗管理」→ 視窗應出現在外接螢幕，點一下進全螢幕；在主視窗拖滑桿、按 1–4、播放潮汐，觀眾視窗應即時跟著變；重整任一視窗數秒內應自動恢復。
4. **觸控筆**：iPad（Apple Pencil）或 Surface / Wacom 在球體上拖曳，重壓的浪勁應明顯大於輕壓；筆向右倒洋流向 X 上升；`?pen=0` 後應無反應。
5. **手把震動**：Chrome / Edge 接手把、按一下任一鍵 → 「裝置 → 觸覺回饋」應顯示「手把：偵測到 1 支，1 支支援震動」→ 手動開啟後按「測試觸感」。
6. **Android 震動**：Android Chrome 開站 →「裝置 → 觸覺回饋」預設為開、顯示「手機震動：支援」→「測試觸感」應依序感受到鯨魚、水滴、淨化波、海豚、海龜、錄製。

### 真機驗證日（建議流程）

把上面所有「尚未驗證」的項目集中在一天做完，並把結果留下證據。

1. **每一台要驗的裝置**（iPhone Safari、Android Chrome、iPad、桌面 Chrome / Edge / Safari / Firefox、接投影機的電腦）都用 **HTTPS** 開 `?diagnostics=1`：先按「執行所有快速檢查」，再把互動檢查逐項按過（相機、麥克風、語音、震動、螢幕清單、全螢幕、MIDI、手把…）。
2. 按「複製報告」或「下載 JSON」存檔，並自己補記裝置型號、系統版本與瀏覽器版本（報告不收相機 / 麥克風 / 螢幕的名稱，也不含電腦 / 手機型號；Web MIDI 埠名稱與手把型號字串則會照實列出）。剪貼簿被擋時改用「下載 JSON」，並記下這個現象。
3. **主畫面**：在 iOS Safari 與 Android Chrome 開站，確認新手導覽 7 步的卡片位置、手機直式演出模式看得到整顆球、資料導覽的 `←` `→` `P`（外接鍵盤）與進度點、「複製此站連結」（貼到另一台裝置開啟）；開「念出字幕」確認聲音、語言與暫停 / 換站的行為，並同時開語音指令，確認旁白不會觸發指令。
4. **展場**：用 `?kiosk=1&reload=<下一個整點的小時>` 讓機器整段時間開著，觀察「裝置 → 維運」（看門狗有沒有誤判、資料檢查、崩潰紀錄）；部署新版後確認頁面在閒置時自動重新載入。
5. 把報告與觀察一併交給接手者；診斷報告可直接貼進 issue。

---

## 語系

介面有**繁體中文**與 **English** 兩個語系。切換：頂部語言鈕（顯示 `EN` / `中文`）；或網址 `?lang=en` / `?lang=zh`。語系決定順序：網址 `?lang=` > 使用者偏好（localStorage `ixd2026.lang`）> 瀏覽器語言（`zh*` 為中文，其餘為英文）。切換語言不用重新整理，資料名稱（水庫、流域、縣市、天氣、農曆、潮差）、日誌、分享文案、新手導覽、導覽字幕、導覽旁白的語言（中文 `zh-TW` / 英文 `en-US`）與語音辨識語言都會跟著換；診斷頁有自己的 `EN` / `中文` 切換鈕，切換不必重跑檢查。測站名與河川名是政府資料的專有名詞，英文版維持原文（英文旁白遇到它們可能跳過或亂念）。

**設計**：以**中文原文當 key**。程式本來就是中文，補譯只要把字面量包進 `t()`；英文放在 `src/i18n/en/*.js`（每個功能一個檔，Vite 以 `import.meta.glob` 自動合併，新增功能不必改任何共用檔）；找不到英文時退回中文原文（開發模式會在 console 記一次）。

**如何新增翻譯**：

1. 把中文字面量包起來：
   - React 元件：`const t = useT()` → `t('分享')`（語系切換時自動重繪）
   - 非 React（純函式、store 日誌）：`import { t } from '.../i18n/index.js'` → 用「當下」語系
   - 靜態表（例如參數登錄表的 `label`）：用 `T('海水高度')` **標記**（原樣回傳中文），顯示時再 `t(label)`
   - 插值：`t('{n} 站', { n: 3 })`；英文值可以是函式 `({ n }) => …` 處理單複數
2. 在 `src/i18n/en/<你的功能>.js` 加一筆 `'中文原文': 'English'`。用語請照 `src/i18n/GLOSSARY.md`（例如全站統一用 sphere，不混用 globe；「分享星球」的按鈕是 Snapshot）。
3. 驗收：`npm run i18n`（等於 `node scripts/i18n-report.mjs`）必須是「未包 t() 的中文字面量 0、缺英文的 key 0、字典重複 key 0」；`npm test` 的 `src/i18n/i18n.test.mjs` 同樣會擋，並額外檢查英文值不含中文、`{placeholder}` 與中文 key 一致。掃描器以 Babel AST 找字面量：註解、`console.*` 與 regex 不算。

**注意事項**：

- 元件若呼叫「讀當下語系的純函式」（`describeBoard`、`formatHud`、`buildTour`…），必須自己 `useT()` / `useLocale()` 訂閱語系，否則切換語言不會重繪。
- 存進 store / 日誌的字串是「當下語系」的快照；要能跟著切換的內容，請存 `{ key, params }`，顯示時再 `t()`（導覽字幕就是這樣做的）。
- 常駐服務若持有和語言有關的狀態（語音辨識的 lang、逐字快取），要監聽 `useLocaleStore.subscribe` 並重設。
- 資料層名稱走 `src/i18n/data.js`（純函式）與 `src/i18n/en/data.js`。
- **說明視窗正文**（`src/ui/info/InfoZh.jsx` / `InfoEn.jsx`）含行內標記（`<b>`、`<code>`），不逐句 `t()`，而是成對維護：兩份的條目順序與事實必須一致；`InfoZh.jsx` 在 `scripts/i18n-check.mjs` 的 `ALLOW_FILES` 內可直接寫中文，**`InfoEn.jsx` 不得含任何中文字元**（i18n 測試會擋）。結構是「快速上手 `QUICK`（最多 5 條）+ 依主題分組的可摺疊區塊 `GROUPS` + 海洋生態」：新增玩法要在兩份的 `PLAY` 陣列各加一筆 `{ id, body }`，並把它的 id 放進 `GROUPS` 裡某一組（每個 id 只能在一組）。`src/lib/onboarding.test.mjs` 會用原始碼解析檢查兩份的 id、分組與行內標記數量一一對應，並釘住目前的條數（19 條玩法 + 5 條快速上手），所以新增玩法時也要一併更新那個測試。說明視窗要保持精簡：只在相關的收合區塊裡補一兩行。

---

## 資料集 → 畫面

資料以政府開放資料（OGDL v1）為主；**「空氣品質」是例外**——它是 Open-Meteo 的 CAMS 模型資料（CC BY 4.0，不是政府觀測值），氣象在沒有 CWA 金鑰時也退回 Open-Meteo。前端只讀靜態的 `public/data/ocean.json`，由 GitHub Actions 定時刷新（沒有後端，金鑰只放在 CI 的 secret）。

| 資料集（來源） | 進到畫面的方式 | 可播放 |
|---|---|---|
| 水庫水情 + 進流量（水利署） | 水位 % ＝ 海水高度、滿庫溢流；進流量 → 洋流 / 魚群 | 24h 進流量 |
| 潮汐預報 F-A0021-001（CWA） | 潮位 → 海水高度（滿潮溢流）；月亮盈虧 ＝ 當日農曆 | 24h 潮位 |
| 河川即時水位（水利署，5 站） | 警戒比 → 背景銀河濃度 | — |
| 鳥類調查（水利署） | 球外鳥群數；逐月季節（環狀內插） | 調查年表（逐年，含空窗年內插） |
| **魚類調查**（水利署 `0a79fde0-a69d-4842-b66b-e51f43ce83f0`） | 「魚群數量」參數；逐月季節 | 調查年表（逐年，含空窗年內插） |
| **IoW 揚塵**（水利署 `ae7bd821-a879-4b65-ad34-b3dac36874e0`，雲林縣濁水溪沿岸 3 站） | PM10 → 海水清澈 / 垃圾 / 色相；風速 → 洋流 | 歷史（見下方限制） |
| **河川流量測站站況**（水利署 `9332bd66-0213-4380-a5d5-a43e7be49255`） | 188 座測站依真實座標排成台灣外形的**背景星座**（現存亮、已廢暗，亮度隨集水面積；同一條河以細線串起）；輕點可看資料出處 | — |
| **月出月沒時刻 A-B0063-001**（CWA，花蓮縣） | 月亮的方位 / 高度（閒置＝今日 × 現在時刻；播放＝逐日） | 逐日（滾動 180 天） |
| **空氣品質（模型資料）**（Open-Meteo Air Quality API，CAMS 全球大氣模型；CC BY 4.0；**不是政府觀測值**） | 雲林縣麥寮（約 23.79°N、120.25°E）的逐時 PM2.5 → 海水清澈 / 垃圾 / 色相 / 輝光；洋流取自今日海況的風速；資料卡另列 PM10 與 US AQI | 逐時歷史（最近 120 小時 ≈ 5 天，每小時 0.2 秒） |

**資料限制（誠實聲明）**
- **揚塵**：IoW 端點只給「最新一筆」，歷史由 CI 每 3 小時追加一筆（以來源時戳去重、保留最近 120 筆 ≈ 15 天），剛部署時只有 1 筆，資料卡顯示「累積中」。目前 PM10 感測器一律回傳哨兵值 4999.4（另有氣溫 1956.96℃ 的離譜值），腳本判為無效 → `null`：此時海況以預設 PM10 = 40 μg/m³ 示意（看板會標「PM10 無效」），歷史播放改以**風速**驅動洋流與海水混濁。來源也可能凍結（時戳前進、數值不變）。截至 2026-09-20 檢查，PM10 仍是無效哨兵值、風速疑似凍結——這是另外加入「空氣品質」的原因。
- **空氣品質（模型資料）**：來源是 Open-Meteo Air Quality API 的 CAMS 全球大氣模型（分析 + 預報），**不是政府測站量到的數字**。網格很粗（數十公里），與地面測站（例如環境部麥寮站）可能有落差，所以只作示意映射，**不可當官方空品判讀**；資料卡、資料看板、日誌與導覽字幕一律標示「模型資料」。`US AQI` 是美國 EPA 指標，不是環境部 AQI，畫面上一律寫「US AQI」。逐時資料，CI 每次以「過去 5 天」覆蓋抓取，與舊資料依時間合併去重，丟掉預報的未來小時，只留「現在以前」最近 120 筆；抓取失敗保留舊資料。洋流不會隨逐時播放變化（歷史沒有逐時風速，只用今日海況的風速）。授權 CC BY 4.0，需標示「Weather data by Open-Meteo.com」並連回 <https://open-meteo.com/>（資料卡有標示與連結，`ocean.json` 的 `air.note` 也寫明歸屬）。Open-Meteo 的免費 API 限非商業使用，若日後商用需改用付費方案。
- **環境部空品資料（可選的後續，尚未實作）**：環境部的空氣品質 API 需要使用者自己申請 API key，本專案沒有代為申請，所以目前沒有接。日後若有 key，可以在 CI 以 `MOENV_KEY` 之類的 secret 接入政府觀測值，屆時「空氣品質」就能改用官方數據；在那之前請把它當作模型資料看待。
- **測站**：只有站點目錄（沒有流量時間序列）；TWD97 二度分帶座標換算成相對座標，僅用於背景星座。
- **月出月沒**：API 預設只回一小段，要帶 `timeFrom` / `timeTo` 才回其他範圍（單次最多 180 天；目前公開 2025-01-01 → 2027-12-31）。CI 取「今日前 2 日起 180 天」的滾動視窗，抓取失敗則保留舊窗口；畫面以 `from / to` 判斷是否涵蓋今日，不涵蓋時閒置的月亮退回天文公式。方位角以正北 0°、順時針；每個陰曆月各有一天沒有月出或月沒。
- **魚 / 鳥調查**：多數流域只有 4–5 個有效月份 / 年度，缺月以相鄰月份環狀內插（標註「內插」）；每月只涵蓋 1–3 個年度，近似單一年份快照，非氣候平均。花蓮溪鳥類 2017–2019 的數量欄位全空（年表的隻次為空）。物種名已正規化（字面 "NULL"、別名順序）。**調查年表是逐年稠密序列**：從第一個到最後一個調查年，每個日曆年一點；沒有調查的空窗年以前後調查年線性內插並標「無調查（內插）」，僅為示意、不是觀測資料，平均 / 基準只用真實調查年。
- 水利署 opendata 在**並行**請求時約 1/4 會回錯置 / 空白 / 截斷的內容，腳本一律依序抓並重試；GitHub runner 連 WRA / CWA 的成功率尚未在 Actions 實測，任何資料集失敗都只會保留舊資料、不影響其他資料集。

**測試**：見下一節〈測試〉（資料相關：`node --test src/lib/series.test.mjs scripts/gov/gov.test.mjs src/lib/air.test.mjs scripts/gov/air.test.mjs`，含對真實 `ocean.json` 的 smoke test）。

---

## 測試

```bash
npm test                              # = node --test：跑全部 *.test.mjs（含 i18n 驗收）
npm run i18n                          # 只看 i18n 報告（0 未包 t()、0 缺英文、0 重複 key 才算乾淨）
node --test src/lib/tour.test.mjs     # 只跑單一檔案
npm run build                         # 建置必須成功
```

以下數字是撰寫本文件時 `node --test` 的實測，各檔測試都是純 Node（不需要瀏覽器；瀏覽器 API 全部以假物件注入）：

| 測試檔 | 項數 | 範圍 |
|---|---|---|
| `src/i18n/i18n.test.mjs` | 8 | i18n 驗收：所有中文字面量都包在 `t()` / `T()`、每個 key 都有英文、字典無衝突、`{placeholder}` 一致且英文值不含中文、函式型英文值、`ocean.json` 名稱在英文模式下皆有譯名、中文模式原樣回傳 |
| `src/lib/i18n-data.test.mjs` | 10 | 資料層文字：中文輸出逐字不變（資料看板 / 日誌 / HUD）、英文措辭、`ocean.json` 全掃英文不含中文、語系切換不殘留 |
| `src/lib/series.test.mjs` | 23 | 資料 → 播放序列與輸出文字；調查年表逐年稠密序列、空窗年內插與 HUD；對真實 `ocean.json` 的 smoke test |
| `scripts/gov/gov.test.mjs` | 14 | 各資料集的離線解析（揚塵哨兵值與歷史滾動、測站座標、魚鳥調查、月出月沒、HTTP 重試、JSON 輸出、烘焙冪等） |
| `src/lib/share.test.mjs` | 35 | 分享連結編碼 / 解碼、資料脈絡（`&o=&m=&sl=&lang=`）、舊連結相容與亂碼容錯、載入順序、圖片分享文案、`fitText` 排版 |
| `src/lib/tour.test.mjs` | 84 | 導覽站（真實 / 缺資料 / 壞資料，含空氣品質站）、倍速、中英字幕無 `undefined` / `NaN`、空窗年、揚塵與空氣品質的誠實說明、還原與各種中斷、循環、偏好、鏡像；導覽員控制（跳站 / 上一站 / 下一站 / 暫停與繼續 / 起始站與 hold）、旁白（假語音） |
| `src/lib/inspect.test.mjs` | 41 | 點物件看資料出處：螢幕空間選取、輕點判定、測站 / 月亮 / 鳥群卡片資料、卡片定位、依內容長度的停留時間 |
| `src/lib/pointerExpr.test.mjs` | 17 | 觸控筆壓力 → 浪勁、傾斜 → 洋流、死區、iOS `altitudeAngle` / `azimuthAngle` 後備換算 |
| `src/lib/audience.test.mjs` | 46 | 觀眾視窗協定（假 BroadcastChannel）：快照 / 增量、節流、斷線偵測、自我修復、選螢幕邏輯 |
| `src/lib/mirror.test.mjs` | 2 | 鏡像狀態切片註冊表 |
| `src/lib/gestures.test.mjs` | 56 | 手勢分類、遲滯、冷卻、平滑、鏡像與左右手、丟手回閒置；假相機的執行期（含分頁隱藏放掉相機與快速切分頁不漏相機、推論慢時的退讓）；`package.json` 與 WASM 版本一致 |
| `src/lib/voice.test.mjs` | 26 | 語音辨識包裝：生命週期、自動重啟、失敗退避、`stop()` 清理、語言切換（假辨識器） |
| `src/lib/voiceCommands.test.mjs` | 53 | 逐字稿正規化、中英同義詞比對（含誤辨與簡體）、去重與冷卻、指令動作 |
| `src/lib/haptics.test.mjs` | 42 | 事件節奏表、強度縮放、冷卻與全域上限、預設值與持久化、手把 dual-rumble（假裝置） |
| `src/lib/quality.test.mjs` | 25 | 畫質狀態機：降 / 升級、遲滯、冷卻、抖動保護、暖機、手動鎖定、偏好序列化 |
| `src/lib/qualityRuntime.test.mjs` | 18 | 畫質執行期：rAF 餵幀、暫停原因（分頁隱藏 / 錄製 / 錄影 / 拖曳）、`?quality=` 覆寫、`stop()` 釋放全部監聽；觀眾視窗自己降級但不寫共用偏好、主視窗的畫質模式鏡像 |
| `src/store/hud.test.mjs` | 1 | 參數 HUD 標籤跟著語系 |
| `src/services/tourCore.test.mjs` | 55 | 導覽 × 真實輸入路徑（真的 `useStore` / `activity` / `createTourRunner`）：MIDI 旋鈕與走帶鍵、語音、手機遙控、滾輪、Marker 都先還原再動作；分頁隱藏中止；閒置計時；導覽期間觸覺靜音；導覽員快速鍵 `←` `→` `P` 的判斷；每站連結只啟動一次 |
| `src/store/rec-stash.test.mjs` | 5 | 使用者的錄製在資料播放 / 整輪導覽之後仍在（拿掉暫存邏輯會失敗） |
| `src/store/survey.test.mjs` | 5 | 分享連結暫時帶來的鳥 / 魚連動不落地、資料播放日誌報真實調查年數 |
| `src/lib/wakeLock.test.mjs` | 6 | 螢幕喚醒鎖：以方法呼叫、請求完成前 release 不洩漏、回前景重新申請 |
| `src/audio/mic.test.mjs` · `src/lib/ar.test.mjs` | 3 · 4 | 麥克風 / AR 相機在授權期間重複點擊、取消、失敗都不留孤兒串流 |
| `src/lib/modalFocus.test.mjs` | 10 | 彈窗焦點管理：開啟移入、Esc、Tab 循環、關閉後焦點回原按鈕、全域快速鍵在彈窗內略過 |
| `src/lib/shareLink.test.mjs` | 6 | 複製分享連結（剪貼簿不可用 → 手動複製）、分享結果訊息的位置 |
| `src/lib/describeGaps.test.mjs` | 4 | 資料看板 / 日誌的鳥魚列標出調查空窗 |
| `src/lib/captionCss.test.mjs` · `scripts/vite-config.test.mjs` · `scripts/sw.test.mjs` | 3 · 13 · 6 | 字幕版面規則（QR 上方、觀眾視窗放大）· 建置設定：分塊（入口不拉 three）、build id（程式碼內容雜湊：只換 `public/data/` 不改 id）與 `version.json` 外掛、`__BUILD_ID__` 注入、`main.jsx` 包錯誤邊界（另有 1 項 dist 驗證，設 `MIDISEA_VERIFY_DIST=1` 才跑，預設略過）· service worker：`/version.json` 不攔截、其他 JSON / HTML 仍離線退回快取、離線不會反覆重載 |
| `src/lib/xr.test.mjs` | 37 | WebXR：特徵偵測（假 `navigator.xr`）、放置數學、hit-test 追蹤、狀態機 idle → requesting → placing → placed → ended、錯誤與系統中斷退場、重複進入 / 退出 |
| `src/lib/urlFlags.test.mjs` | 2 | 網址旗標（?kiosk / ?audience）一致：`=0` / `false` / `off` 明確關閉，導覽與觀眾視窗判斷同一個函式 |
| `src/lib/diagnostics.test.mjs` | 57 | 診斷頁：25 項快速檢查（假 `navigator` / `window`）、互動檢查探測器的資源釋放（track / 辨識 / `AudioContext` / 計時器 / 監聽）、報告（Markdown + JSON）與隱私過濾、會檢查 `this` 的假物件（脫離物件的原生呼叫會讓測試轉紅） |
| `src/lib/air.test.mjs` · `scripts/gov/air.test.mjs` | 16 · 20 | 空氣品質：資料 → 播放序列 / 映射 / 看板與 HUD 文字（中英、「模型資料」標示、任何顯示處都不說它是政府資料）/ `playAir`；抓取端的請求網址、驗證、逐時解析、歷史合併去重與 120 筆上限、選項參數、失敗時保留舊資料；對真實 `ocean.json` 的檢查 |
| `src/lib/resilience.test.mjs` | 84 | 展場防呆：網址旗標 → 設定、崩潰紀錄（環狀 / 去重）、退避 5 → 15 → 60 秒與 10 分鐘 5 次熔斷、同一瞬間多筆致命事件併成一次、看門狗、WebGL 遺失復原、版本檢查與資料更新（忙碌 / 閒置）、每日重載、ErrorBoundary 與維運面板；預設環境在瀏覽器 `this` 規則下可用 |
| `src/lib/layoutPrefs.test.mjs` | 18 | 系統事件監看：預設依寬度、偏好讀寫、`?log=` 優先序、視窗跨過 820px 跟隨、`localStorage` 不可用時退回預設、Footer 最新 OUT 事件與節流 |
| `src/lib/onboarding.test.mjs` | 49 | 新手導覽：何時自動顯示（含 `?onboard=`、`?kiosk`、`?tourstop=`）、記住的鍵、聚光與卡片定位幾何、狀態機與鍵盤、焦點進出、`InfoZh` / `InfoEn` 結構一一對應 |
| `src/lib/cameraFit.test.mjs` | 28 | 視角取景：各長寬比的倍率（寬螢幕恆為 1，逐位元不變）、相機狀態機（第一幀就位、尺寸變才重算、`?fit=0`）、以 three 的 `PerspectiveCamera` 實際投影、星座與星殼配置 |
| `src/lib/tourLink.test.mjs` | 18 | 每站連結：`?tourstop=` / `?tourhold=` 解析、產生連結時不夾帶其他旗標、往返、複製到剪貼簿（失敗時退回舊寫法） |
| `src/lib/narration.test.mjs` · `src/lib/echoGuard.test.mjs` | 45 · 3 | 旁白（假 `speechSynthesis` / 假計時器）：`speak` 的完成 / 取消 / 錯誤、挑聲音、單位與符號口語化 · 回音防護：旁白進行中與結束後短時間內忽略語音指令 |
| `src/audio/timing.test.mjs` | 3 | 溢流水滴的排程時間單調遞增（避免 Tone.js 丟「time must be >= last scheduled」） |

合計 **954 項**（`node --test`：953 項通過、1 項跳過；跳過的是需設 `MIDISEA_VERIFY_DIST=1` 才跑的 dist 驗證；共 41 個測試檔，含上表 WebXR 37 項）。

單元測試的限制：Node 看不到瀏覽器專屬的錯（例如 `this` 綁定造成的 `Illegal invocation`），所以每個新功能仍需要在真實瀏覽器跑過一次——這也是〈驗證狀態〉一節存在的原因。

---

## 專案結構

```text
IXD2026/
├─ README.md · README.en.md · SPEC.md   # 說明（繁中 / English）· 完整規格（含 Mermaid）
├─ index.html · package.json · vite.config.js   # vite.config.js 另含 version.json 外掛（輸出 dist/version.json 並注入 __BUILD_ID__）
├─ .github/workflows/                   # deploy.yml（GitHub Pages 部署）· refresh-data.yml（定時刷新 ocean.json）
├─ public/                              # data/ocean.json（資料快照）· sw.js · manifest · icons · CNAME
├─ tools/midi-monitor.html              # MIDI 偵測器（實測 CC/Note、匯出映射）
├─ scripts/
│  ├─ fetch-ocean-data.mjs              # CI 刷新：氣象 / 潮汐 / 揚塵 / 月出月沒 / 空氣品質（各自 try/catch，失敗保留舊資料）
│  ├─ bake-static-data.mjs              # 一次性烘焙：測站目錄 / 魚類調查 / 鳥類逐年（冪等）
│  ├─ tide.mjs                          # 潮汐公開檔解析
│  ├─ gov/                              # 每個資料集一個純函式模組 + 離線測試（node --test）；air.mjs＝Open-Meteo 空氣品質（模型資料）
│  ├─ i18n-check.mjs                    # i18n 靜態掃描（Babel AST）：未包 t() 的中文字面量、缺英文的 key
│  └─ i18n-report.mjs                   # npm run i18n 的入口（呼叫 i18n-check）
└─ src/
   ├─ main.jsx                          # 進入點：主畫面 App / 手機遙控頁（#remote=）/ 診斷頁（?diagnostics=1）/ 觀眾視窗（?audience=1）四選一，最外層包 ErrorBoundary
   ├─ App.jsx · AudienceApp.jsx · DiagnosticsApp.jsx · styles.css
   ├─ ErrorBoundary.jsx                 # 最外層錯誤邊界：倒數自動重載 · 退避 · 熔斷 · WebGL / 看門狗提示（展場防呆）
   ├─ hooks/useMIDI.js                  # Web MIDI 連線 + 訊息解析
   ├─ store/                            # useStore.js（Zustand 單一狀態源：參數/綁定/錄製/soft-takeover）· activity · events · hud · stats
   ├─ params/registry.js                # 參數分組 + 預設綁定
   ├─ audio/                            # engine.js（Tone.js 生成式背景音）· mic.js（麥克風＝風）· bus.js · timing.js（溢流水滴的排程時間）
   ├─ remote/RemoteApp.jsx              # 手機遙控頁（輕量，不載入 three）
   ├─ i18n/                             # 語系
   │  ├─ index.js                       # t / T / useT / useLocale / setLocale
   │  ├─ data.js                        # 資料層名稱翻譯（水庫 / 流域 / 農曆 / 潮差…，純函式）
   │  ├─ GLOSSARY.md                    # 英文用語表（所有翻譯者共用）
   │  ├─ i18n.test.mjs                  # 驗收測試
   │  └─ en/*.js                        # 英文字典：每個功能一檔，建置時自動合併
   ├─ services/                         # 常駐服務（Services.jsx 一次掛載）：Audience / Gesture / Voice / Haptics / Quality / Tour / Resilience（tourCore.js＝導覽的非 React 接線，Node 可測）
   ├─ lib/
   │  ├─ persist.js                     # localStorage / sessionStorage（所有偏好的 key 都在這裡）
   │  ├─ share.js                       # 參數 ↔ 分享網址、資料脈絡（?s= &o= &m= &sl= &lang=）、分享文案 / 排版
   │  ├─ capture.js                     # 分享卡 / 錄影（實景時合成相機背景）
   │  ├─ ar.js                          # 相機背景 · 環境光取樣 · 濾鏡
   │  ├─ series.js · describe.js        # 資料 → 播放序列（含調查逐年序列 / 空窗）/ 輸出顯示文字（純函式）
   │  ├─ govdata.js · moon.js · birds.js  # 載入 ocean.json · 月相與月出月沒位置 · 鳥群季節
   │  ├─ tour.js · tourLink.js          # 資料導覽：導覽站 / 字幕 / 執行器 / 導覽員控制（純邏輯）· 每站連結（?tourstop=）與複製
   │  ├─ narration.js · echoGuard.js    # 導覽旁白（speechSynthesis 包裝、單位口語化）· 回音防護（旁白時忽略語音指令）
   │  ├─ inspect.js · pointerExpr.js    # 點物件看資料出處 · 觸控筆壓力 / 傾斜
   │  ├─ diagnostics.js · diagnosticsSummary.js   # 診斷頁：檢查註冊表 / 探測器 / 報告 · 給「裝置」面板讀的小摘要模組（不拉診斷本體）
   │  ├─ resilience.js                  # 展場防呆：網址旗標 → 設定 · 崩潰紀錄 · 退避與熔斷 · 看門狗 · WebGL 復原 · 版本檢查 · 資料更新 · 每日重載
   │  ├─ layoutPrefs.js · onboarding.js · cameraFit.js   # 系統事件監看偏好 · 新手導覽（步驟 / 幾何 / 狀態機）· 視角自動取景（純函式）
   │  ├─ urlFlags.js                    # 網址旗標的共同判斷（?kiosk / ?audience / ?diagnostics …）
   │  ├─ mirror.js · audience.js        # 鏡像狀態切片註冊表 · 觀眾視窗協定（BroadcastChannel）
   │  ├─ wakeLock.js · modalFocus.js · shareLink.js · remoteDispatch.js   # 螢幕喚醒鎖 · 彈窗焦點管理 · 複製連結（含手動複製後備）· 手機遙控訊息派送
   │  ├─ gestures.js · hands.js         # 相機手勢：分類與狀態機 · 相機 + MediaPipe 執行層
   │  ├─ voice.js · voiceCommands.js    # 語音辨識包裝 · 指令表與比對
   │  ├─ haptics.js                     # 觸覺回饋（手機震動 / 手把 dual-rumble）
   │  ├─ xr.js                          # WebXR 桌面放置：特徵偵測 · 放置數學 · hit-test · 狀態機（純邏輯，動態載入）
   │  ├─ quality.js · qualityStore.js · qualityRuntime.js   # 自動畫質：狀態機 · 共用狀態 · 執行期
   │  ├─ midiRoute.js · blemidi.js · multiplayer.js · sensors.js · ice.js   # MIDI 路由 · 藍牙 MIDI · 多人合奏 · 手機感測器 · WebRTC ICE
   │  └─ *.test.mjs · tourTestEnv.mjs   # 單元測試（與被測模組同資料夾）· 導覽測試用的假環境（假計時器 / 剪貼簿 / 旁白）
   ├─ ui/                               # TopBar · ParamPanel · Monitor · Footer · Splitter · DataCard · DataBoard · DataHUD · SurveyTimeline
   │  │                                 # TourControls · TourCaption · TourNav · Onboarding · InspectCard · KioskQR · InfoModal · MultiModal · DevicesModal · XrOverlay …
   │  ├─ devices/                       # 「裝置」面板的各節：AudienceSection · GestureSection · VoiceSection · HapticsSection · QualitySection · XrSection · DiagnosticsSection · OpsSection
   │  └─ info/                          # 說明視窗正文：InfoZh.jsx · InfoEn.jsx（成對維護）
   ├─ styles/                           # 各功能 CSS（air · audience · diagnostics · gestures · haptics · inspect · monitor · onboarding · quality · resilience · timeline · tour · tourpresenter · voice · xr）
   ├─ scene/Scene3D.jsx                 # R3F 3D 星球
   ├─ scene/XrRuntime.jsx               # WebXR 的 R3F 執行層（session 存在時才動態載入）
   └─ timeline/scenes.js                # 場景預設
```

---

## 技術

React + Vite · Three.js + React Three Fiber · Zustand · Web MIDI API · Tone.js · cannon-es · PeerJS（WebRTC）· MediaPipe Tasks Vision（相機手勢，動態載入）· Web Speech API（語音指令辨識、導覽旁白的語音合成）· BroadcastChannel（觀眾視窗）。

**效能設計**：3D 場景在 render loop 內以 `useStore.getState()` 讀參數，MIDI 高頻訊息不觸發 React re-render；錄製緩衝與 soft-takeover 狀態放模組層，避免每則訊息重繪。手機遙控頁與觀眾視窗各自只載入需要的程式碼（遙控頁的入口只依賴 react，約 80 KB gzip，不含 three / 場景 / store；主畫面與觀眾視窗一開始就預載 3D 場景）；MediaPipe 只在使用者打開相機手勢時才載入；沒有觀眾視窗連著、沒有開啟手勢 / 語音時，對應的服務不做任何工作。

**開發輔助**：dev 模式下主控台可用 `window.__store` 驅動測試。

---

## 資料 / 授權

政府資料開放授權條款第 1 版 (OGDL v1)。完整資料來源與 schema 見 [SPEC.md](./SPEC.md)。

**Open-Meteo（CC BY 4.0，需標示出處）**：「空氣品質」資料與氣象備援來自 [Open-Meteo](https://open-meteo.com/)，以 CC BY 4.0 授權；使用時須標示「Weather data by Open-Meteo.com」並連結到 <https://open-meteo.com/>——資料卡的空氣品質區塊已附這行標示，連結指向 Open-Meteo 空氣品質 API 的文件頁（`https://open-meteo.com/en/docs/air-quality-api`，來自 `ocean.json` 的 `air.sourceUrl`），`air.note` 也寫明歸屬。空氣品質是 CAMS 全球大氣模型資料，**不是政府觀測值**，也不適用 OGDL v1。Open-Meteo 的免費 API 限非商業使用；本專案只由 CI 每 3 小時抓一次（瀏覽器不直接呼叫），若日後商用需改用付費方案。

**資料管線**：`public/data/ocean.json` 為建置時快照（水利署水庫水情 + 24h 進流量時序 + 氣象 + 河川水位 + 鳥 / 魚調查 + 揚塵 + 測站目錄 + 月出月沒 + Open-Meteo 空氣品質模型資料），GitHub Actions 每 3 小時（另在台北 00:10 跨日後）排程 `scripts/fetch-ocean-data.mjs` 刷新並自動 commit，**刷新後主動派發 deploy 工作流程**（GitHub 的設計是 `GITHUB_TOKEN` 的 push 不會觸發其他 workflow）。歷史靜態資料（測站目錄 / 魚類調查 / 鳥類逐年）由 `scripts/bake-static-data.mjs` 一次性烘焙，CI 原樣保留。
- **潮汐 series 每日自動更新，不需要金鑰**：抓 CWA F-A0021-001「潮汐預報」公開檔（未來一個月、266 個地點），取花蓮市乾滿潮事件（海圖基準）做餘弦插值成當日 24h 潮位；併入上次保存的前一日尾端事件，頭段曲線才精準（首次以相鄰半週期鏡射補頭）。有 `CWA_KEY` 時改走官方 datastore API（同一份資料）；兩者都失敗則保留舊 series。
- **空氣品質不需要金鑰**：`scripts/fetch-ocean-data.mjs` 每次刷新以 `past_days=5` 抓 Open-Meteo 的逐時資料，與舊資料合併去重、只留最近 120 小時；抓取失敗只保留舊資料。環境部空品 API 需要自己申請的 key，目前沒有接（可選的後續，見〈資料集 → 畫面〉）。
- 在 repo **Settings → Secrets and variables → Actions** 加入 `CWA_KEY`（[中央氣象署開放資料平臺](https://opendata.cwa.gov.tw/)申請的授權碼）→ 氣象也改抓 CWA 觀測站（政府源）；未設定時氣象自動退回 Open-Meteo。

**隱私總覽**：網站沒有後端、沒有帳號，程式碼中沒有任何分析追蹤（沒有 Google Analytics 一類的腳本）。相機影像、麥克風聲音都不會被本站錄下或上傳（語音辨識在 Chrome / Edge 是由瀏覽器把聲音送到雲端服務，見〈語音指令〉）；偏好與錄製只存在你自己瀏覽器的 localStorage / sessionStorage。相機手勢第一次啟用會向 jsDelivr 與 Google 儲存空間下載模型與運算程式（只有下載，沒有上傳）。「多人」合奏使用 PeerJS 的公共訊號伺服器建立 WebRTC 連線，連線受 NAT 或會場 Wi-Fi 擋住時會經公共 TURN 中繼（`openrelay.metered.ca`，見 `src/lib/ice.js`）；經過的只有滑桿數值與觸發事件，不含影像或聲音。

**本輪新增功能的隱私**：裝置診斷頁的所有檢查都在你的瀏覽器裡執行、報告只在你按「複製」或「下載」時產生，且不含個人資料、IP、影像或音訊，也不收相機 / 麥克風 / 螢幕的名稱；但 Web MIDI 埠名稱與手把型號字串會照實列出（判讀有沒有偵測到控制器用，貼出報告前請看一眼）；崩潰紀錄只存在這台裝置的 localStorage（`ixd2026.crashes`），不會上傳；新手導覽、系統事件監看與視角取景只在本機記一個偏好或做計算；導覽旁白由瀏覽器的語音合成負責，本站不傳送任何文字（瀏覽器若選用雲端聲音，是否連網由瀏覽器與系統決定）。版本檢查與資料自動更新只向本站自己的 `version.json` 與 `data/ocean.json` 發出請求。
